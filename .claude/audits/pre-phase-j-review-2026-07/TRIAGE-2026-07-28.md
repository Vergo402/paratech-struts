# Stage 0 Triage — Residual Medium/Low Findings — 2026-07-28

Adjudication of the 38 Medium/Low findings from `REPORT.md` (2026-07-04) that had no
tracking issues (the 8 Highs + 7 security Mediums were tracked as #415–#429 and closed
2026-07-10). Method: 3 parallel read-only triage agents (sonnet) with per-finding
code-anchored verdicts; FIXED verdicts independently spot-checked on the main loop.
Baseline at triage time: 1390/1390 tests green, lint clean, typecheck clean — the
report's baseline stragglers (stale `nativeControls` test, `shoreSafety.test.ts` lint
error) were already resolved.

**Result: 4 FIXED · 34 OPEN.** All 34 survivors filed as board issues #447–#480,
nested under their Phase J gate or a punch-list bucket (#445 sync/offline, #446
correctness/UX, both under epic #138).

## Fixed since the report (no issue needed)

| Report finding | Fixed by | Evidence |
|---|---|---|
| Cross-view length collision (List/Division raw opening) | `39f1363` (#416) | `cardParts.ts` `cardValueEighths()` feeds Board/List/Division alike |
| `deleteRole` proceeds when `readMembers()` fails | `ff673c9` (2026-07-10) | null-read guard returns error before remove |
| ADMIN_MANAGE self-promotion escalation | `57b7689` | `adminGainingAdmin` gate in `rules.ts:249-259` |
| Audit CSV formula injection | `57b7689` | `formulaSafe()` on all four peer-controlled columns |

## Open survivors → issues

- **Safety/deploy → sub-issues of #260 (SME gate):** #447–#457 (8 Medium, 3 Low) —
  DeployResolution ack holes (#447–#449), deduction commit-before-deploy (#450),
  partial-deploy honesty (#451), pinned-row group deploy (#452), group timeline gaps
  (#453), split-status Division tile (#454), zero-load "ok" (#455), stale ack (#456),
  plateHeight 0″ (#457).
- **Accessibility → sub-issues of #258 (WCAG gate):** #458–#462 (2 Medium, 3 Low).
- **Security UI/rules mismatch → sub-issue of #257:** #463 (Low).
- **Sync/offline/import → bucket #445:** #464–#474 (7 Medium, 4 Low).
- **Correctness/UX → bucket #446:** #475–#480 (2 Medium, 4 Low).

Each issue body carries the current file:line anchor, defect, and failure scenario;
this file is the index, the issues are the working detail.

## Lower-confidence verdicts (flagged by the triage agents)

- #451 (partial-announce): the "all-fail closes silently" half is certain; the
  partial-success message is truthful about count but omits the shortfall.
- #458 (SR lockstep count): mechanism confirmed; call-site pre-filtering not
  exhaustively ruled out.
- #463 (UI/rules mismatch): file renamed to `MemberEditSheet.tsx`; lineage inferred
  from its own doc-comment.
- #471/#472 (sync wedge / badge replay): mechanisms confirmed; no external
  dead-letter path or joiner-exclusion path was found, but not exhaustively ruled out.
