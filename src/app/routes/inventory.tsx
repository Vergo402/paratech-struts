// Thin route wrapper — the screen lives in @ui/inventory (operations parity).
// router.tsx imports InventoryScreen from here, so this re-export keeps that path.
export { InventoryScreen } from '@ui/inventory';
