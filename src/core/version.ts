// The app's build-identity string — the single source for the header tag, the
// SideNav footer, the About page, and the feedback payload. Lives in core so any
// layer (data/feedback included) can read it without an upward import. Keep in
// sync with package.json `version` on each release.
export const APP_VERSION = 'v4.0.0-slice.1';
