// core/operation — the operation projection + reducer (L-7 group fan-out).
// Pure: no React, no Firebase. Consumed by ui/operations and data/store.
export * from './reducer';
export * from './projection';
