// data/sync — the one backend path (stub behind the real seam this slice).
export {
  syncService,
  createSyncService,
  type SyncServiceApi,
  type RowSyncState,
  type ReconcileResult,
} from './syncService';
