# S12 handoff — archive record

Received 2026-06-11 from the Claude Design S12 card-design round.
Brief that commissioned it: [`../S12-design-brief.md`](../S12-design-brief.md).
Decisions are implemented **in-code** per the Phase H kickback fix-plan §S12 — **this archive is the record of what came back, the live code is the truth.** Where the two diverge, the code wins.
The `.txt` suffixes on the component sources are intentional: they stop the build from compiling these reference prototypes. Read them in place or strip the suffix when implementing.
Brand SVGs under `assets/logo/` are stored **as delivered** (independent of what the slice adopts): the F5-fixed `mark-mono.svg` + `favicon.svg` were adopted into the canonical set; the emblem family was kept at the repo version (delta was serialization-only).
