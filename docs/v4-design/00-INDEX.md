# FieldShore v4 Design Folder Index

> Scannable table of contents. Updated by every commit. The full plan lives at `/Users/alex/.claude/plans/v4-master-plan.md` (outside the repo, so it survives branch churn).

---

## Status Legend

| Status | Meaning |
|---|---|
| 🟢 Done | Committed, reviewed, accepted |
| 🟡 In progress | Being written |
| 🚦 Gate ready | Awaiting Alex review |
| ⚪ Not started | Planned, not yet begun |
| 🔴 Blocked | Waiting on a decision or dependency |

---

## Phase Status

| Phase | Description | Status | Notes |
|---|---|---|---|
| A | Foundation: branch, folder, seed files, Project setup, /v4-plan skill | 🟢 Done | All seed files committed. Project items converted from drafts to real Issues (#129 through #138). Open questions #1, #2, #17 resolved. Demo mode added to Bucket 1. |
| B | Reference app teardowns + positioning doc | 🟢 Done | Structural deliverables shipped 2026-05-21 (6 teardowns + positioning.md). Sign off review closed 12 follow ups by 2026-05-22. Positioning doc rewritten in Alex's voice and trimmed to ~1,450 words (commit e7f9bb3, 2026-05-23). Gate B passed 2026-05-23 after adding Principle 12 (ADR-004) out of #280, so Phase C briefing packet carries the data class framing. |
| C | 12-agent brainstorm essays | 🟢 Done | Gate C passed 2026-05-24. All 12 essays reviewed; no changes requested. Phase D dispatched. All 12 essays committed 2026-05-23 (59,559 words total; target was 2,750–3,000 per essay, several came in higher because the lens earned it, no padding flagged). Each essay carries the 250-word executive summary and numbered recommendations Phase D copies verbatim into the coverage matrix. Notable: essay 8 (skeptical-review) dissents against ADR-003 and ADR-004 via numbered recs (permitted); essay 11 (scenario-stress) flags sibling-essay predictions that are likely to tear at Hamden/Meadowville scale; essay 4 surfaces 12 data-model schema fields that must be reserved in v4 to avoid v5 migration. |
| D | Synthesis + decision tracking matrix | 🟢 Done | Synthesis + coverage matrix committed 2026-05-24; revised 2026-05-31 after Alex's PR #282 review (18 comments folded in). Matrix: 218 accepted / 13 deferred / 15 rejected / 1 merged. The two held research items resolved 2026-05-31: NIMS org structure → **ADR-008** (`04-references/nims-org-structure.md`, all 7 questions answered), database → **ADR-009** (`04-references/database-evaluation.md`, stay on Firebase RTDB). Gate D passed; squash-merged to `v4-redesign` via PR #282. Phase E unblocked. |
| E | Design system (color, type, spacing, motion, primitives, etc.) | 🟢 Done | **Phase E cascade gate PASSED — Alex, 2026-06-07.** The full design system (8 token/system docs + **14 primitives** + ADR-010–013) is signed off; the 2026-06-07 audit closed the pre-gate gaps (see below) before sign-off. **Gate trio APPROVED by Alex 2026-06-01** ([#292](https://github.com/Vergo402/paratech-struts/issues/292)): `color.md` + `typography.md` + `03-primitives/card.md` (four-theme contrast-verified via `wcag-contrast.mjs`) + **ADR-010** (status commit model, amends Principle 6) + **ADR-011** (color token system) + **ADR-012** (measurement precision → 1/8″, floor-rounded, SME-confirmed) — all three ADRs now Accepted. **Living styleguide** at `preview/` (token-driven HTML, 4 themes × 4 widths, fraction-style toggle) is the rendered proof. Card carries: RecommendationCard (1/8″ digit-pair fractions, indented deduction ledger, capacity demoted) + ShorePointCard (deployed strut cradle-to-grave, pending = v3 Assign-Equipment model). **Cascade now unblocked** — ~19 files (spacing → motion → primitives → cross-cutting), one issue per session. `spacing-grid.md` (E2) ✅ delivered ([#177](https://github.com/Vergo402/paratech-struts/issues/177)); `motion.md` (E3) ✅ delivered ([#178](https://github.com/Vergo402/paratech-struts/issues/178) — drops the timed-undo line per ADR-010); `iconography.md` (E4) ✅ delivered ([#179](https://github.com/Vergo402/paratech-struts/issues/179) — four-size grid, stroke tokens, ~46-glyph inventory, shore type diagram doctrine requirement); `logo-and-mark.md` (E6) ✅ delivered ([#180](https://github.com/Vergo402/paratech-struts/issues/180) — **new identity, full color**: an "FS" emblem built from a gold LongShore + 2 aluminum struts (rigid 6″ base/sole plates) and a cut-4×4/plywood-gusset S, + a simplified mono mark (themed, 16px-safe) + app-icon/favicon/wordmark; [**ADR-013**](https://github.com/Vergo402/paratech-struts/blob/v4-redesign/docs/v4-design/11-decisions/ADR-013-brand-emblem-full-color.md) exempts the emblem from the one-accent rule); `voice-and-tone.md` (E5) ✅ delivered ([#181](https://github.com/Vergo402/paratech-struts/issues/181) — copy doctrine: creed *terse · doctrine-aligned · never cute*; ratifies the seven status display labels `color.md` deferred here (incl. renames **Strut Set** / **Shore Secured** / **Strut Equipment Returned**); how-the-app-talks before/after rules; NIMS terminology lock via the "Chief Test"; per-surface + accessibility + anti-patterns; **mints no CSS tokens**, doc-only); `accessibility.md` (E7) ✅ delivered ([#182](https://github.com/Vergo402/paratech-struts/issues/182) — authored last, the consolidation point: **WCAG 2.1 AA floor / AAA where reachable**; consolidates all six siblings' a11y sections *by reference, not restatement*; the load-bearing **"assistive tech cannot slide"** rule (focusable Advance/Step-back button equivalents + `aria-live` announce + **Power Select** native-`<select>` fallback on VoiceOver/TalkBack-or-Settings); a screen-reader **script registry** (*Role · Name · State · Action-hint* grammar + scripts for the 2 filled primitives + global patterns; #183–196 extend it); modal focus-trap / skip-link / keyboard parity; haptics survive reduced-motion; **mints no CSS tokens**; lean baseline per Alex, formal SC-by-SC audit deferred to Phase H/J). **All 8 design-system token/system docs now complete; next: the primitive cascade — fill `03-primitives/*.md` at picker depth ([#183](https://github.com/Vergo402/paratech-struts/issues/183)–[#196](https://github.com/Vergo402/paratech-struts/issues/196)).** **Cascade started:** `sheet.md` ✅ delivered ([#183](https://github.com/Vergo402/paratech-struts/issues/183) — bottom-sheet surface primitive: sheet-vs-modal as a rule, 3 variants incl. the preserved v3 plate/wood grid, v3.5.1 iOS hardening carried forward as doctrine; **mints no tokens**; `--scrim` flagged for `modal.md`). `modal.md` ✅ delivered ([#184](https://github.com/Vergo402/paratech-struts/issues/184) — center-anchored stop surface: modal-vs-sheet as a rule, 4 variants, **mints the shared `--scrim`** in `color.md` resolving sheet.md OQ1, heavy confirmation reserved for the destructive/terminal per ADR-010). `badge.md` ✅ delivered ([#186](https://github.com/Vergo402/paratech-struts/issues/186) — read-only indicator vocabulary: ~24 v3 classes → 5 variants, badge-vs-interactive-chip boundary (chip → `input.md`), **mints no tokens**, the one primitive that renders on broadcast). `toggle.md` ✅ delivered ([#190](https://github.com/Vergo402/paratech-struts/issues/190) — the binary switch: **toggle-vs-segmented + toggle-vs-checkbox as a rule** (the v3 `.system-toggle` Gold/Grey/LockStroke is *multi-select*, so neither a toggle nor a segmented → `input.md` filter chips); 2 variants (setting toggle = immediate+reversible per Principle 6 / form toggle = staged with its form); **state carried by thumb position, not color** (Principle 9); off is the safe default (Principle 5); **mints no tokens** — cites `--accent` on / neutral off / `--surface-card` thumb / `--motion-micro` flip / 56·48pt row; OS-native-switch fallback; SR script added to `accessibility.md`). `button.md` ✅ delivered ([#187](https://github.com/Vergo402/paratech-struts/issues/187) — the action primitive: **button-vs-not-a-button as a rule** (nav tab → navigation, `.system-toggle` (multi-select) → `input.md` filter chips, `.qty-btn`/`.theme-option` → `segmented.md`, `±` stepper → `input.md`, slide-to-advance → `card.md`, link vs button); 32 v3 button classes → **3 emphasis levels + 1 destructive overlay** (primary / secondary / tertiary + danger); primary fills with the **one accent gold** — v3's blue/green/red console palette retired, emphasis is fill→outline→text; **flags `--on-accent`** for `color.md` to mint (the per-theme primary-fill foreground, contrast-verified) — its lone token dependency; icon-button = secondary/tertiary only (Principle 9) + **FAB / arc speed-dial retired** (Principle 4); `guardClick` → intrinsic double-fire guard; Advance/Step-back = the slide's accessible-equal button; SR scripts added to `accessibility.md`). `loading-state.md` ✅ delivered ([#195](https://github.com/Vergo402/paratech-struts/issues/195) — the honest wait: **loading-vs-sync-indicator + loading-vs-empty as a rule** (the ambient sync dot is instant/non-blocking → `color.md`/`badge.md`; not-yet ≠ nothing, the boundary lives in `aria-busy` → `empty-state.md`); **4 treatments** — busy control (v3 `guardClick`/`.btn-loading`) / skeleton / determinate progress / inline spinner; **local-first makes a loader the exception**, static-first, determinate-over-indeterminate, failure resolves inline not via `alert()`; canonizes v3's two field rulings — feedback-on-every-tap (Surfside IP-010) + static-over-animated; **mints no tokens** — the unowned continuous-loop timing (spinner rotation/shimmer) flagged for the slice; SR scripts registered in `accessibility.md`). `empty-state.md` ✅ delivered ([#194](https://github.com/Vergo402/paratech-struts/issues/194) — the calm zero-state surface: **empty-vs-loading-vs-error as a three-way rule** (settle-before-empty; a safety/rating omission defers to the warning card, never a neutral "no results"; the unfilled org slot → `card.md`); **4 variants by why-it's-empty** — first-run (one create) / filtered (clear-filter; may escalate to the would-fit informational `RecommendationCard`) / upstream-blocked (points elsewhere) / all-clear (success, no CTA); centered void-stack — muted set-glyph `--icon-size-lg`/`xl` @40% + headline + reason + ≤1 button (Principle 4/7/11); **mints no tokens** — cites `--icon-size-*` / `--text-secondary` / `--space-*`, never `--danger`; retires v3's 7 nothing-here renderings; no appearance animation; SR script registered in `accessibility.md`). `slider.md` ✅ delivered ([#192](https://github.com/Vergo402/paratech-struts/issues/192) — the slide-to-commit control governing the **ADR-010 status advance**: **slider-vs-value/toggle/segmented as a rule** + **v4 ships no value-range slider** (measurement = 1/8″ keypad per ADR-012); 2 configs (advance leading-edge→right / step-back trailing-edge→left, secondary + permanent, not a timed undo); **commit-past-threshold + snap-back** = the ghost-tap defense, the drag is direct-manipulation; **status-only domain** — deploy/return are buttons→`warning-gate`, never slides; the travel reveals the **destination status hue** (matches the badge cross-fade); **mints no tokens**; `card.md` owns placement / `slider.md` owns the control; the load-bearing *assistive-tech-cannot-slide* button-equivalent contract). `input.md` ✅ delivered ([#189](https://github.com/Vergo402/paratech-struts/issues/189) — the data-entry primitive: **input-vs-picker as a rule** (v3 `<select>` → picker, checkbox → toggle, 1×–4× qty → segmented); 7 variants — text / text-area / numeric / measurement / search / chip-tag-input / multi-select filter chips; receives the **interactive chip** (`.app-chip`+`×`) from `badge.md`, the **measurement-display fraction component** from `typography.md`, and the **multi-select filter** (the v3 `.system-toggle`) from `segmented.md`; measurement field = 56pt gloved keypad + 1/8″ stacked fractions; validation → inline `aria-invalid`, never color-alone; reversible edits never confirm; **mints no tokens**). `segmented.md` ✅ delivered ([#191](https://github.com/Vergo402/paratech-struts/issues/191)); `nested-checklist.md` ✅ delivered ([#196](https://github.com/Vergo402/paratech-struts/issues/196)); `list.md` ✅ delivered ([#188](https://github.com/Vergo402/paratech-struts/issues/188)); `toast.md` ✅ delivered ([#193](https://github.com/Vergo402/paratech-struts/issues/193)). **The primitive cascade is filled at picker depth — 13 files ([#183](https://github.com/Vergo402/paratech-struts/issues/183)–[#196](https://github.com/Vergo402/paratech-struts/issues/196)) plus `warning-gate.md` ([#293](https://github.com/Vergo402/paratech-struts/issues/293), added at the 2026-06-07 audit) = 14 at the Phase E gate; the **side-drawer** ([#306](https://github.com/Vergo402/paratech-struts/issues/306), ADR-019) added at the Phase F #217 gate brings the live set to 15.** *(Bookkeeping: on 2026-06-06 the remaining issues were run concurrently; the #188 / #190 / #193 files were left uncommitted and reconciled in a cleanup commit, while `input.md` was committed twice and `empty-state.md` swept into input's commit — cosmetic, history left as-is.)* **Primitive cascade ready for the Phase E gate.** **Phase E audit (2026-06-07):** closed the gate-blocking gaps a pre-gate read surfaced — (1) [`warning-gate.md`](03-primitives/warning-gate.md) ([#293](https://github.com/Vergo402/paratech-struts/issues/293)) **written** (4 primitives referenced the accepted K-11 primitive but it was never authored; 9 links were dangling); (2) `--on-accent` + `--shadow-modal` **minted** in [`color.md`](07-design-system/color.md) (cited as real in button/modal anatomy but flagged un-minted), `wcag-contrast.mjs`-verified, [ADR-011](11-decisions/ADR-011-color-token-system.md) §Addendum; `--motion-loop` **registered to Phase H** (not minted — `motion.md` caps durations at six); (3) the destructive-confirmation links that mis-routed to `warning-gate` now point to [`modal.md`](03-primitives/modal.md) (its territory); (4) the cascade's per-doc open questions are **reconciled into [`99-open-questions.md`](99-open-questions.md)** (#19–#23) so the gate is no longer blind to them. |
| F | Information Architecture per screen × 4 surfaces | 🟢 Done | **Phase F gate ([#217](https://github.com/Vergo402/paratech-struts/issues/217)) PASSED — Alex, 2026-06-09.** All 19 screen specs + the cross-cutting foundation are signed off; all five gate follow-ups (#304–#308) resolved before sign-off. Next: Phase G (workflow design). **Foundation laid 2026-06-07.** `08-information-architecture/00-ia-foundation.md` (the "picker.md of Phase F") + **ADR-014** (keep the v3 5-tab spine; nest), **ADR-015** (fixed bottom nav, push drilldown, progressive across surfaces, guest-first cold-open), **ADR-016** (ratify the sheet/modal doctrine, apply per-screen) + `_TEMPLATE.md` — all three ADRs **Accepted** — **foundation mini-gate PASSED (Alex, 2026-06-07)**, #214/#215/#216 closed. The tab map locks where every screen lives; **4 new sub-issues filed** — [#294](https://github.com/Vergo402/paratech-struts/issues/294) Cutting Station, [#295](https://github.com/Vergo402/paratech-struts/issues/295) Org Chart, [#296](https://github.com/Vergo402/paratech-struts/issues/296) Hazard Log, [#297](https://github.com/Vergo402/paratech-struts/issues/297) Roster; **#212 Demo Mode closed** (dropped). OQ #4 + #26 resolved. **Session 1 (2026-06-07): Operations ([#199](https://github.com/Vergo402/paratech-struts/issues/199)) + Cutting Station ([#294](https://github.com/Vergo402/paratech-struts/issues/294))** (`20-operations.md` + `21-cutting-station.md`); **Session 2 (2026-06-07): Quick Find ([#198](https://github.com/Vergo402/paratech-struts/issues/198)) + Inventory ([#200](https://github.com/Vergo402/paratech-struts/issues/200))** (`10-quick-find.md` + `40-inventory.md`); **Session 3 (2026-06-07): Command/SitStat ([#201](https://github.com/Vergo402/paratech-struts/issues/201)) + Org Chart ([#295](https://github.com/Vergo402/paratech-struts/issues/295)) + Hazard Log ([#296](https://github.com/Vergo402/paratech-struts/issues/296))** (`30-command-sitstat.md` + `31-org-chart.md` + `32-hazard-log.md`); **Session 4 (2026-06-07): IC Command Checklist ([#203](https://github.com/Vergo402/paratech-struts/issues/203)) + Task Level Checklist ([#204](https://github.com/Vergo402/paratech-struts/issues/204)) + ORM/TCRM ([#205](https://github.com/Vergo402/paratech-struts/issues/205))** (`33-ic-command-checklist.md` + `22-task-level-checklist.md` + `23-orm-tcrm.md`) — all compose `nested-checklist`; **Session 5 (2026-06-08): Roster→Accountability ([#297](https://github.com/Vergo402/paratech-struts/issues/297)) + Settings ([#202](https://github.com/Vergo402/paratech-struts/issues/202))** (`41-accountability.md` + `50-settings.md`); **Session 6 (2026-06-08): the pre-shell auth cluster — Login/Register ([#206](https://github.com/Vergo402/paratech-struts/issues/206)) + Dept Setup ([#207](https://github.com/Vergo402/paratech-struts/issues/207)) + Invite Code ([#208](https://github.com/Vergo402/paratech-struts/issues/208))** (`70-login-register.md` + `71-dept-setup.md` + `72-invite-code.md`) — guest-first pre-shell routes; **Session 7 (2026-06-08): the admin cluster — User Manager ([#209](https://github.com/Vergo402/paratech-struts/issues/209)) + Cross-Dept Invite ([#210](https://github.com/Vergo402/paratech-struts/issues/210), v4.5) + Audit Log ([#211](https://github.com/Vergo402/paratech-struts/issues/211))** (`51-user-manager.md` + `52-cross-dept-invite.md` + `53-audit-log.md`); **Session 8 (2026-06-08): Broadcast View ([#213](https://github.com/Vergo402/paratech-struts/issues/213))** (`60-broadcast-view.md`) — the read-only projection mode / registry over all 18 screens. **All 19 screen specs done — Phase F screen specs COMPLETE; full phase gate = [#217](https://github.com/Vergo402/paratech-struts/issues/217).** **#217 gate walked (Alex, 2026-06-08) — approved-with-changes:** Tier-1 applied (CH-1 load-demote · CH-2 multi-saw note · **CH-5 Roster→Accountability rename** · CH-6 staleness→synced/pending · CH-8 Audit Log IC/Ops-only · Ops Begin-Briefing); 5 follow-ups under #134 ([#304](https://github.com/Vergo402/paratech-struts/issues/304) RBAC (ADR-017 + 51 rewrite — **closed**) · [#305](https://github.com/Vergo402/paratech-struts/issues/305) auto-email (**ADR-018** — closed) · [#306](https://github.com/Vergo402/paratech-struts/issues/306) side-drawer primitive (**ADR-019** — closed) · [#307](https://github.com/Vergo402/paratech-struts/issues/307) Excel — 10-column schema + Flatfile-style import + CSV (**closed**) · [#308](https://github.com/Vergo402/paratech-struts/issues/308) Settings — Administration gating matrix + new **Department policies** group (after-action toggle, ADR-018) + **Settings-by-context** role view; OQ2/OQ4 resolved (**closed**)). **All five gate follow-ups (#304–#308) closed; [#217](https://github.com/Vergo402/paratech-struts/issues/217) gate PASSED (Alex, 2026-06-09) — Phase F COMPLETE.** |
| G | Workflow specs across all surfaces | 🟢 Done | **PHASE G COMPLETE — gate [#239](https://github.com/Vergo402/paratech-struts/issues/239) PASSED (Alex, 2026-06-09).** All 20 workflow specs + foundation; ADR-021 (command-transfer handshake) + ADR-022 (mutual aid → v4.0). Field review pass-with-changes → every blocker fixed and **independently re-verified RESOLVED** (18 Resolved · 13 deferrals Accepted · 0 Block · 0 Not-resolved; reports [`_PHASE-G-GATE-REVIEW.md`](09-workflows/_PHASE-G-GATE-REVIEW.md) + [`_PHASE-G-GATE-REVIEW-VERIFICATION.md`](09-workflows/_PHASE-G-GATE-REVIEW-VERIFICATION.md)). The 5 must-fix decisions applied + mutual aid pulled into v4.0; doctrine spine (no-push / no-safety-hold) verified intact. **Next: Phase H (vertical slice — now including the mutual-aid path).** **Gate closeout (2026-06-09):** **Workstream A** encoded the 5 must-fixes — **[ADR-021](https://github.com/Vergo402/paratech-struts/blob/v4-redesign/docs/v4-design/11-decisions/ADR-021-command-transfer-handshake.md)** two-party command-transfer handshake (outgoing retains command until incoming accepts; never a no-IC state) · ICS-201 brief = live six-datum snapshot in v4.0 (not empty) · End-Op unreturned-equipment warning v4.0 · hazard→SP Division-level fallback · task-checklist per-Group/per-task attach-target. **Workstream B = [ADR-022](https://github.com/Vergo402/paratech-struts/blob/v4-redesign/docs/v4-design/11-decisions/ADR-022-mutual-aid-v40-qr-guest.md): mutual aid v4.5 → v4.0** (QR-everywhere join + guest participation for un-provisioned units; merged multi-agency roll-up now v4.0; supersedes the ADR-003 *deferral*, scope unchanged) — rewrote `32`/`52` to v4.0, ripples in `06`/`07`/`08`/`20`/`31`/`41`/`53`, reopened #235/#210 as v4.0; **the v4.0 slice expanded (master plan flagged)**. Should-fix → Phase H trackers 99-OQ #37–#42. Gate review §10 records every resolution. **Remaining gate step: Alex's #239 sign-off.** **Phase G STARTED 2026-06-09.** Foundation-first, mirroring E/F. **Foundation laid:** `09-workflows/00-workflow-foundation.md` (the "picker.md of Phase G" — workflow-vs-screen, **Mermaid `stateDiagram-v2`** as the locked state-diagram notation, the text-wireframe frame, the **cross-surface actor-surface matrix** (Phase G's distinct job — *who acts on which device when*, propagation via the event log on sync, never a push), reversibility cited from ADR-010 (no timed-undo), accessibility-reuse, the cite-vs-own litmus test, file-naming bands) + `_TEMPLATE.md`. **Worked example:** `05-setting-up-inventory.md` ([#218](https://github.com/Vergo402/paratech-struts/issues/218)) authored to full depth as the foundation's proof. **No new ADR** — Phase G inherits ADR-008/010/012/015/016 (foundation is format + depth-bar, like `voice-and-tone.md`). Epic [#135](https://github.com/Vergo402/paratech-struts/issues/135) open through the phase. Full sub-issue set #218–#239 filed. **Session 1 (2026-06-09):** foundation + #218 ✅. **Session 2 (2026-06-09):** `10-starting-an-operation.md` ([#219](https://github.com/Vergo402/paratech-struts/issues/219)) + `11-adding-a-shore-point.md` ([#220](https://github.com/Vergo402/paratech-struts/issues/220)) ✅ — the slice-critical entry pair; #219 and #220 closed. **Session 3 (2026-06-09):** the full status arc — `12-deploying-a-strut.md` ([#221](https://github.com/Vergo402/paratech-struts/issues/221)) + `13-cutting.md` ([#222](https://github.com/Vergo402/paratech-struts/issues/222)) + `14-runner.md` ([#223](https://github.com/Vergo402/paratech-struts/issues/223)) + `15-secured-returned.md` ([#224](https://github.com/Vergo402/paratech-struts/issues/224)) ✅ — the complete SP lifecycle from equipment assignment through terminal return; #221–#224 closed. **Session 4 (2026-06-09):** the command-side cluster — `16-end-of-operation.md` ([#238](https://github.com/Vergo402/paratech-struts/issues/238)) + `20-role-assignment-command-transfer.md` ([#225](https://github.com/Vergo402/paratech-struts/issues/225)) + `21-hazard-log.md` ([#226](https://github.com/Vergo402/paratech-struts/issues/226)) + `22-ic-command-checklist.md` ([#227](https://github.com/Vergo402/paratech-struts/issues/227)) + `23-task-level-checklist.md` ([#228](https://github.com/Vergo402/paratech-struts/issues/228)) ✅ — the lifecycle's lone terminal action, the org/command surface, the no-safety-hold hazard register, and the two side-drawer attestation checklists; #225–#228 + #238 closed. **Session 5 (2026-06-09) — final content session:** the D7 auth cluster — `06-signing-in-and-out.md` ([#234](https://github.com/Vergo402/paratech-struts/issues/234)) + `07-department-setup.md` ([#231](https://github.com/Vergo402/paratech-struts/issues/231)) + `08-joining-by-invite-code.md` ([#232](https://github.com/Vergo402/paratech-struts/issues/232)) — plus `24-tcrm-briefing.md` ([#229](https://github.com/Vergo402/paratech-struts/issues/229)) + `25-customizing-checklists.md` ([#230](https://github.com/Vergo402/paratech-struts/issues/230)) + `30-user-management.md` ([#233](https://github.com/Vergo402/paratech-struts/issues/233)) + `31-audit-log-review.md` ([#236](https://github.com/Vergo402/paratech-struts/issues/236)) ✅ — #229–#234 + #236 closed. **Plus the mutual-aid invite+accept workflow `32-mutual-aid-invite-accept.md` ([#235](https://github.com/Vergo402/paratech-struts/issues/235), **v4.5 — shown-but-disabled in v4.0**, specced now for parity + to name the multi-agency deferral plainly per gate review B3).** **All 20 workflow specs done** (05/06/07/08/10–16/20–25/30/31/32); **#237 Demo Mode REJECTED/closed** (cut at the Phase D gate — not a deliverable). **Phase G gate (#239) field review DONE 2026-06-09** — an 8-reviewer cold first-impression panel (3 Chiefs command + 4 line officers trench, all zero-experience + design-rationale-withheld, + 1 doctrine-aware battalion-chief gate lens) → consolidated report at [`09-workflows/_PHASE-G-GATE-REVIEW.md`](09-workflows/_PHASE-G-GATE-REVIEW.md). **Recommendation: PASS-WITH-CHANGES** (8/8 "trust-with-changes," 0 reject; 6 blockers / 14 majors). 5 must-fix *decisions* before Phase H (command-transfer closed-loop handshake; ICS-201 brief not-empty; End-Op unreturned-equipment warning; hazard→SP matching fallback; task-checklist attach-target). **Awaiting Alex sign-off.** Then: gate close + Phase H. ✅ **#230 scope resolved 2026-06-09 ([ADR-020](https://github.com/Vergo402/paratech-struts/blob/v4-redesign/docs/v4-design/11-decisions/ADR-020-department-checklist-authoring.md)):** Alex's call — **departments fully author** their checklists (FieldShore ships a paraphrase-approved baseline; depts fork + tailor; **provenance** marks baseline-vs-dept-authored so Principle 1 stays intact). `25-customizing-checklists.md` rewritten from curate-only to the authoring model; **the one earlier open gate-flag is now closed.** |
| H | Vertical slice prototype | 🚦 Gate ready for review | **Foundation batch (2026-06-09):** the build-stack ADRs that gate the slice are drafted — [ADR-005](11-decisions/ADR-005-single-package-pwa.md) (single-package PWA), [ADR-006](11-decisions/ADR-006-reserved-schema-fields.md) (reserved schema fields), [ADR-007](11-decisions/ADR-007-build-system-typescript-strict.md) (Vite + TS strict), [ADR-023](11-decisions/ADR-023-component-state-stack.md) (Radix/Tailwind/TanStack/Zustand/Zod), [ADR-024](11-decisions/ADR-024-d5-multi-device-build-a.md) (D5 → Build A default; Build C → v5), [ADR-025](11-decisions/ADR-025-authentication-implementation.md) (email+password + magic-link) — **all six 🟢 Accepted — foundation mini-gate PASSED (Alex, 2026-06-10), ratifying the Phase-D-gate stack.** Closes #240 / #242 / #243 / #244 / #245 + #309 (ADR-006); resolves OQ #4b / #7 / #8 / #11 / #12 / #13. **Pre-slice scaffold DONE (2026-06-10):** `07-design-system/architecture.md` (the map) + `module-boundaries.md` (the seam constitution) committed (#310 closed; OQ #24/#25 resolved). **Slice build [#246](https://github.com/Vergo402/paratech-struts/issues/246) STARTED — Session 1 (2026-06-10): toolchain + core domain DONE.** Vite + TypeScript-strict + the three ESLint boundary invariants + the Tailwind-v4/token bridge stood up; typecheck/lint/test/build all green and the v3 root app untouched (Vite root = `src/app`, output → gitignored `dist/`). Pure `core/*` built + tested: `core/schema` (Zod single source of truth), `core/load` (the **selection engine + load tables ported VERBATIM from v3 `app.js`** — L-1 conservative floor + L-2 deduct-once-floor-the-display, pinned by snapshot + property tests), `core/shorepoint` (`STATUS_ORDER` + `canTransition` guard + per-SP reducer + `findForShorePoint`), `core/operation` (event-log `projectOperation` + L-7 group fan-out via the lockstep rule). **37 Vitest tests green**; the 3 boundary invariants proven to trip on violation. Scope forks settled with Alex: data/sync = local stub behind the seam · all 3 field affordances adopted (#37 / #20+#38 / #40) · Quick Find = shared inputs only. **Session 2 (2026-06-10): data layer DONE.** `data/store` built + tested: `db.ts` (Dexie/IndexedDB — `events` append log keyed by auto-increment `seq` for total fold order, `id` unique-indexed; `inventory`; `meta`), `operationStore.commit` (the only legal mutation entry: Zod-validate → durable append **before** the in-memory update → enqueue — L-4; deploy/return run in one Dexie transaction with the L-8 invariants: abort-on-missing-node, abort-on-none-available, clamp `available ≤ quantity`, ID round-trip), `auth.ts` (per-device uid minted under `fieldshore_auth_uid`, ADR-024), `seed.ts` (synthetic 17-item fixture — every #247 verification path drivable and **pinned by tests against the real engine**: 30″ recommendations / 16″ no-match / 200″ unrated warning / zero-available rig), `boot.ts` (uid → seed-if-empty → fold the log through `projectOperation`). `data/sync` = the stub behind the real seam: real `enqueue/flush/reconcile/getRowSyncState` signatures, flush no-op, **the L-7 merge guard built for real** (a peer status event premised on state older than ours is dropped before it enters the log; a peer move premised on our exact current status — including a deliberate ADR-010 step-back — applies; merged events don't re-enqueue). `ui/hooks` = the one sanctioned `@data` importer (`useOperation`/`useShorePoints`/`useInventory`/`useRecommendations`/`useCommit`, zustand-vanilla external-store sync — the L-4 synchronous hot path; TanStack Query joins with real sync states). **63 Vitest tests green (37 + 26 new)**; invariant-3 lint proven to trip; splash boots the real data layer in-browser (17 items seeded, console clean). **Session 3 (2026-06-10): primitives + pickers + quickfind inputs + themed shell DONE.** `ui/primitives` = the Phase-E set as code, doctrine MUST-rules structural: Badge (7 locked status words on the per-theme `--status-*` pairs; cross-fade only on post-mount commit; sunlight banner / broadcast text-only escalations in CSS), Button (intrinsic double-fire guard — async press self-disables + `aria-busy`; disabled always carries its reason), Card (flat, `edge` slot ready for the S5 stripe), EmptyState (4 variants; all-clear refuses an action), WarningGate (the three doctrine sentences as frozen constants; unrated ack unlocks deploy and keeps the disclosure; over-capacity stays closed), Modal/Sheet on Radix Dialog + a one-overlay-at-a-time singleton (destructive modal auto-focuses the safe default; sheet carries the L-9 iOS CSS on its scroll body), Toggle (Radix Switch), Segmented (Radix RadioGroup, in-place indicator), Slider (custom slide-to-commit, `shouldCommit` pure helper, **#37 visible Advance/Step-back button built into the primitive**), Measurement (stacked ⅛″ digit-pair fraction renderer). `ui/picker` = the four variants: InlineSegmented, BottomSheetPicker (immediate commit), FullScreenList (search appears >7; ships as overlay — route-push deferred, no 8+ consumer yet), PowerSelect placeholder, and **VisualGridPicker — the v3 plate picker verbatim (L-9): always-mounted `createPortal` grid, `visibility` toggle (never unmount), `touch-action: pan-y` + `translateZ(0)` + overscroll containment, Esc/scrim/outside close, selected scrolls into view, v3 #121 in-stock sections; no focus trap (verbatim; AT path = PowerSelect)**. `ui/quickfind` = the settled #20/#38 affordances: MeasurementInput (big mono readout + ft/in steppers + 8-cell eighths tap-strip; exact-int eighths only; inline 0/360″ clamp messages — no keypad this slice, deviation logged) + DeductionPicker (fixed ledger Opening→Header→Top→Bottom→Footer→Effective; math = core `effectiveLengthFrom`, zero UI constants). `src/app` shell: ThemeProvider (`fieldshore_theme`, pre-paint script — no wrong-theme flash; system via matchMedia), TanStack Router 5-tab spine + stubs, fixed BottomNav (no animation), Settings theme picker live, **`/gallery` dev surface (URL-only) renders every primitive for preview verification + the #248 gate**. Geist + Geist Mono self-hosted (Fontsource; token stacks updated in canonical + copy same-commit). Toolchain traps fixed: vitest/eslint `.tsx` test globs (S3 tests would have silently never run), jsdom storage polyfill. **127 Vitest tests green (63 + 64 new)**; typecheck/lint/prod build + PWA clean; production preview drove: 4-theme live flip, L-9 grid (open/commit/Esc/scrim), measurement strip+steppers+bounds, ledger math exact (68⅝−3.5=65⅛), modal focus/Esc, sheet handle, slider #37 commit. **Session 4 (2026-06-10): workflow #219 Start Operation DONE.** `ui/operations` = the first real workflow through the full stack: **`OperationsBoard.tsx`** (no-op empty-state → `EmptyState` variant="first-run" + Start Operation CTA; active-op header with `<h1>` name + pencil edit button; **7 status lanes** via `STATUS_ORDER` with `Badge` variant="count" + collapsible `Lane` sub-component (ephemeral `Set<ShorePointStatus>`, `aria-expanded` chevron rotation via CSS); `+ Add Shore Point` primary button stub; `End Operation` secondary destructive → placeholder modal with disabled button) + **`StartOperationModal.tsx`** (one component, two modes: create = "Start Operation" / edit = "Edit Operation" + "Save"; form = `TextField` name (required, `disabledReason`), `Toggle` multi-building, `TextField` location; create dispatches `OperationCreated` event via `useCommit()` → `commitHaptic()`; edit computes diff → `OperationEdited` with only changed fields, location cleared = `null`; no changes = `onClose()` immediately; form reset via `useEffect` on `open`) + **`operations.css`** (`fs-ops-*` / `fs-lane-*` classes on design tokens) + **`useDeviceUid`** hook (thin boundary-legal wrapper) + barrel + gallery demo section. `operations.tsx` route wired. **144 Vitest tests green (127 + 17 new)**; typecheck/lint/prod build clean; preview drove: empty state → create op → board with 7 lanes → lane collapse/expand → edit pre-populated → reload persistence → End Op stub → no console errors. **Session 5 (2026-06-10): workflow #220 Add Shore Point DONE.** The board's spine is live end-to-end: **`AddShorePointModal.tsx`** (create + edit in one component, S4 pattern; field order Building-if-multi → Division → Area → Shore type `InlineSegmented` → Quantity (create only, always visible — v3-faithful per Alex; >10 warns, never blocks) → MeasurementInput → DeductionPicker inline → Label; **grouped qty N → N `ShorePointAdded` events sharing a `groupId` committed as ONE atomic batch**; edit = diff-only `ShorePointEdited` patch, no-change Save closes commit-free, cleared optionals send `null`; 3-Post auto-fills 6×6 wood, T-Shore/Double-T never auto-fill and switch-away never resets — the v3.9.1 lesson; last-used division/type/building seed from the newest SP) + **`ShorePointCard.tsx`** (presentational; div-variant Card — never `onPress`, buttons are siblings; **4pt status stripe + 16pt full-height tap zone in the Card `edge` slot** — a real labeled button on Pending firing Assign Equipment, decorative `span` after; identity line via `divisionLabel` + `MeasurementValue` digit-pair + group `n / total` badge + status badge; deployed-strut model + source cradle-to-grave; Pending block = "No equipment assigned" + `pendingReason` line only-when-set + full-width Assign Equipment; tap-to-expand head reveals Edit / Delete, Pending only) + **`DeleteShorePointModal.tsx`** (destructive confirm, Cancel auto-focused, → `ShorePointDeleted`) + **`DivisionPicker.tsx`** — **the v3 grow-the-building division model ported now (Alex: fixed list = regression)**: `Operation.divisions: number[]` (init `[1]` in the reducer, never on the wire) + new **`DivisionAdded` event** (additive + idempotent — concurrent adds converge, no array clobber) + pure `core/operation/division.ts` (`formatDivision` v3-verbatim: "Div 1 (Ground level)" / "Sub Div 1 (Basement)", next-floor math, cross-section sort) + a Sheet-composed picker with "+ Add floor above / below" committing + selecting. Data layer: **`operationStore.commitMany`** (validate ALL → reject inventory-consequential → one Dexie `bulkAdd` transaction, all-or-nothing on duplicate-id, ONE setState, enqueue each) + `useCommitMany` hook; `ShorePointPatch` building/area/label now `nullable` (null clears — the `OperationEdited.location` convention; reducer `delete`s on null, label-clear works post-Pending). **Found + fixed a structural overlay gap S5 exposed first:** the S3 one-overlay-at-a-time claim closed the host modal when a picker opened inside it — `overlay.ts` is now a **parent→child claim stack** (an overlay whose opener sits inside the current overlay's container STACKS; peers still close each other — the v3 plate-in-modal pattern made structural); Modal/Sheet gained Esc-peels-one-layer + inside-child-isn't-outside-dismiss guards; pinned by `overlay-nesting.test.tsx`. Board wiring: real cards in lanes, add/edit/delete modals, post-add Pending-lane auto-expand + scroll-to-card + assertive `aria-live` announce ("3 shore points added — Div 1, Pending."). Logged S5 stubs/deviations: Assign Equipment = no-op until S6; slide-to-advance = S6; DeductionPicker inline not collapsed-disclosure (Principle 7); focus-landing on new card skipped (scroll + announce only); sunlight stripe uses `--sp-text` (white) — `--sp-solid` remap deferred cosmetic. **209 Vitest tests green (144 + 65 new)**; typecheck/lint/prod build clean; dev + production preview drove: single add → Pending card w/ stripe; **T-Shore ×3 → [1/3][2/3][3/3] one batch, same `at`, lane expands + announces**; division add-above/below persist + survive reload; plate grid inside the modal opens/picks/Esc-peels with the modal staying open; edit prefilled (qty hidden) → diff-only patch; delete confirm Cancel-focused → card gone; 3-Post 6×6 autofill verified; multiBuilding gates the Building field; op + SPs + divisions all refold from Dexie on reload; v3 root untouched. **Session 6 (2026-06-10): workflow [#221](https://github.com/Vergo402/paratech-struts/issues/221) Deploy Strut DONE.** The full deploy arc built: **`RecommendationCard`** (the v3 `renderResults()` anatomy per card.md — COLOR—SYSTEM header + strut-color accent bar (gold→`--accent`, grey→ink, no new tokens), model, adjusted range, extension block, the **rigid Opening→Header→Top Connector→Bottom Connector→Footer→Effective ledger** with always-shown slots, N/S-in-danger for unselected, ≈-nearest-⅛ plate display with exact-math footnote, Effective promoted in the strut color + floored-note; capacity stays demoted; disclaimer gate on every card; **unrated → WarningGate ack unlocks Deploy, disclosure persists** (v3 NEW-2 verbatim); **over-capacity → Deploy closed outright + `is-gated` #40 danger treatment** (warning-gate.md doctrine — the workflow doc's lumped ack reading yields to the primitive); fully-extended zero-margin caution carried (F-4B-1)). **`AssignEquipmentSheet`** (picker sheet per ADR-016 — SP context header, `useRecommendations`, per-rig duplicate results each with **`Equipment from:` resolved via the combo's `inventoryId`** (engine `StrutCandidate` type now exported — type-only edit, snapshots untouched), single-flight deploy lock (two in-flight deploys would double-decrement past the store pre-flight), commit-fail inline `role=alert` keeps the sheet open, deployed model string composes extensions — "LS 1016 + 12″" cradle-to-grave). **ShorePointCard In Process** = advance Slider ("Slide to set Strut Set" + **#37 Advance/Step-back buttons**, Slider gained `disabledReason`) + secondary step-back slide; **Strut Set step-back ships too** (Alex-approved scope add — ADR-010 always-reversible; advance→Cutting stays #222). **`StepBackConfirmModal`** — un-deploy is the ONE confirming reversal (inventory-consequential, ADR-016): "Return LS 203 to inventory?" → `StrutReturned` restores stock. **Group gate (workflow OQ2 implemented):** a grouped In-Process card's advance is disabled-with-reason ("Waiting on group — 2 of 3 still Pending") until every mate leaves Pending; deploys stay per-member; one slide then fans out all lockstep mates via the reducer ("3 shore points — now Strut Set." announced). **Live `pendingReason` (Alex-approved):** new pure `core/shorepoint/pendingReason.ts` computes no-match vs no-inventory from CURRENT stock (v3's informational re-run repurposed; deliberately broader than v3's empty-table stamp — copy pass owns the nuance, OQ1) — the Pending card's reason line is finally real and self-clears as stock changes; the sheet's empty states reuse it (no-match `filtered` / no-inventory `upstream-blocked`). Board grew a **polite status live region** (assertive stays add-only), deploy/advance/step-back lane auto-expand + scroll, and **live-by-id sheet/modal derivation** (stale-object fix; self-closes if the point leaves the actionable state). Logged S6 stubs/deviations: extension/plate stock NOT decremented (strut item only — Phase I); acknowledged-unrated gate does not ride the deployed card (needs a `DeployedStrut` field — Phase I, 20-operations.md flag); Quick Find escalation link omitted (route is a stub); Power Select fallback still placeholder; **#247 state 5 nuance: `exceedsCapacity` can't fire in the sheet (SP load is always 0) — in-sheet gate proof = unrated 200″; the #40 over-capacity card is engine-driven in `/gallery`** — flag to Alex at the gate. **256 Vitest tests green (209 + 47 new)**; typecheck/lint/prod build clean; production preview drove all six #247 states end-to-end: 30″ happy path (deploy → identity + **IndexedDB decrement 4→3** → Strut Set → step-back), grouped ×3 (independent deploys, gate on/off, one-slide fan-out), un-deploy confirm (stock restored), 16″ no-match + exhausted-LS-1016 no-inventory (card reason + sheet empty states live), 200″ unrated in-sheet (ack unlocks Deploy, sheet never dismisses, "LS 1016 + 12″" composite identity deploys); dev server smoke green; v3 root untouched. **Session 7 (2026-06-10): verification + gate prep DONE — 🚦 GATE READY, Alex drives ([#248](https://github.com/Vergo402/paratech-struts/issues/248)).** Formal [#247](https://github.com/Vergo402/paratech-struts/issues/247) pass on a FRESH production bundle (DB wiped, seed re-ran, single-context-verified): **all six states PASS** — record at [`13-slice/_PHASE-H-SLICE-VERIFICATION.md`](13-slice/_PHASE-H-SLICE-VERIFICATION.md) with 15 screenshots + the re-runnable headless capture script ([`13-slice/capture-screenshots.mjs`](13-slice/capture-screenshots.mjs)); quality floor green (256 tests / typecheck / lint / build; v3 root provably untouched since fork-point commits). Bonus proofs on camera: live `pendingReason` self-clear on stock return; Strut-Set step-back fans the group back (pre-cutting lockstep both directions); un-deploy stays individual. **Alex decisions (2026-06-10): (1) Option 1 — `/gallery` is the accepted proof surface for the #40 over-capacity card** (SP load is always 0 this slice; gate mechanics proven in-sheet via unrated 200″; re-verify in-sheet when the load field ships); **(2) raker clarification recorded** — Paratech direct vertical-shoring chart tops at 12′, the 16′ O&M figures are raker territory (much-later feature, nothing planned); the 200″ unrated seed is a mechanism test, not a doctrinal vertical shore. Gate package for Alex: [`13-slice/_PHASE-H-GATE-SCRIPT.md`](13-slice/_PHASE-H-GATE-SCRIPT.md) — plain-language drive script + the **consolidated 14-item deviations register** swept from S1–S6 (extension/plate stock; unrated-ack not riding the deployed card; focus-landing; Quick Find/Inventory/Command stubs; End-Op placeholder; keypad; plate swatches; Power Select; ledger disclosure; sunlight stripe; overlay-not-route; no advance-to-Cutting; sync stub). **Gate verdict pending: pass → Phase I; kick-back → fix list.** Epic [#136](https://github.com/Vergo402/paratech-struts/issues/136). |
| I | Whole-app build (months) | ⚪ Not started | Blocked by H |
| J | Cutover to main | ⚪ Not started | Blocked by I |

---

## Resolved Backlog: Phase B follow ups

Twelve issues filed from Alex's sign off review of 2026-05-21 (eleven on review day, one retroactive). All closed as of 2026-05-22.

| # | Title | Closed |
|---|---|---|
| [#269](https://github.com/Vergo402/paratech-struts/issues/269) | positioning: Replace The Sentence with Alex's rewrite | 2026-05-21 |
| [#270](https://github.com/Vergo402/paratech-struts/issues/270) | positioning: Tone down pass across the whole document | 2026-05-21 |
| [#271](https://github.com/Vergo402/paratech-struts/issues/271) | positioning: Fix First Due paragraph | 2026-05-21 |
| [#272](https://github.com/Vergo402/paratech-struts/issues/272) | positioning: Fix Tablet Command paragraph | 2026-05-21 |
| [#273](https://github.com/Vergo402/paratech-struts/issues/273) | positioning: Reframe defensibility | 2026-05-21 |
| [#274](https://github.com/Vergo402/paratech-struts/issues/274) | positioning: Rewrite for voice; name Level III through I expansion | 2026-05-22 |
| [#275](https://github.com/Vergo402/paratech-struts/issues/275) | positioning: Reclassify RapidSOS as UX/UI reference | 2026-05-22 |
| [#276](https://github.com/Vergo402/paratech-struts/issues/276) | positioning: Acknowledge DRR Rescue in the in rubble bullet | 2026-05-22 |
| [#277](https://github.com/Vergo402/paratech-struts/issues/277) | ADR-002: Principle 1 scope clarification | 2026-05-22 |
| [#278](https://github.com/Vergo402/paratech-struts/issues/278) | ADR-003: Scope, everyday plus expandable | 2026-05-22 |
| [#279](https://github.com/Vergo402/paratech-struts/issues/279) | Plain language cleanup of all v4 artifacts | 2026-05-22 |
| [#281](https://github.com/Vergo402/paratech-struts/issues/281) | terminology: rename incident complexity from Type to Level (retroactive sub issue of #274) | 2026-05-22 |
| [#280](https://github.com/Vergo402/paratech-struts/issues/280) | Design Foundation: structural collapse is a different data problem (resolved at Gate B via Principle 12 + ADR-004) | 2026-05-23 |

---

## File Index

### Foundation (Phase A)

- `01-context.md` why this redesign exists 🟢
- `02-principles.md` the 12 design principles 🟢 (Principle 12 added 2026-05-23 via ADR-004, out of #280)
- `03-primitives/picker.md` UI picker doctrine (worked example of design depth) 🟢
- `11-decisions/ADR-template.md` template for Architecture Decision Records 🟢
- `99-open-questions.md` rolling list of unresolved questions 🟢

### Primitives (Phase E will fill these out)

- `03-primitives/picker.md` 🟢 (seeded in Phase A)
- `03-primitives/card.md` 🟢 (Phase E gate primitive — APPROVED 2026-06-01; status-stripe tap zone, slide-to-advance, red-slash off-queue, inline deduction ledger, capacity demoted, deployed-strut cradle-to-grave, pending = v3 Assign model)
- `03-primitives/sheet.md` 🟢 (#183 — the bottom-anchored slide-up surface: **sheet-vs-modal as a rule**; 3 variants incl. the preserved v3 plate/wood grid picker; tokens cited not minted (`--radius-sheet` / `--shadow-sheet` / `--motion-transition`); v3.5.1 iOS hardening carried forward as doctrine; focus-trap + "assistive tech cannot drag" Close equivalent; scrim-token flagged for `modal.md`)
- `03-primitives/modal.md` 🟢 (#184 — the center-anchored stop surface: **modal-vs-sheet as a rule**; 4 variants (destructive/terminal confirm, inventory-consequential confirm, full-screen form, blocking alert); **mints the shared `--scrim`** in `color.md`, resolving sheet.md OQ1; heavy confirmation reserved for the destructive/terminal per ADR-010; v3's 9 overlays + 29 `alert()`/`confirm()` re-sorted (most are not v4 modals); focus-trap + inert background close the v3 gap; `--shadow-modal` flagged for the slice)
- `03-primitives/badge.md` 🟢 (#186 — the read-only indicator vocabulary: **badge-vs-interactive-chip as a rule** (chip → `input.md`); ~24 v3 badge classes collapse to **5 variants** — status, count, label/tag, severity, indicator dot; **mints no tokens** — cites `--radius-badge` / `--space-2` / `--type-body-medium` / `--status-*` / `--motion-status`; status badge = label-word + per-theme escalation (tint → sunlight banner → broadcast border) + commit-only cross-fade; the one primitive that fully renders on broadcast; registers its + the four other variants' SR scripts in `accessibility.md`)
- `03-primitives/button.md` 🟢 (#187 — the action primitive: **button-vs-not-a-button as a rule** (nav tab → navigation, `.system-toggle` (multi-select) → `input.md` filter chips, `.qty-btn`/`.theme-option` → `segmented.md`, `±` stepper → `input.md`, slide-to-advance → `card.md`, link vs button); 32 v3 button classes → **3 emphasis levels + 1 destructive overlay** (primary / secondary / tertiary + danger); primary fills with the **one accent gold** — v3's blue/green/red console palette retired, emphasis is fill→outline→text; **flags `--on-accent`** for `color.md` to mint (the per-theme primary-fill foreground, contrast-verified) — its lone token dependency; icon-button = secondary/tertiary only (Principle 9), **FAB / arc speed-dial retired** (Principle 4); `guardClick` → intrinsic double-fire guard; Advance/Step-back = the slide's accessible-equal button; SR scripts registered in `accessibility.md`)
- `03-primitives/list.md` 🟢 (#188 — the arrangement primitive: **list-vs-item + list-vs-picker as a rule** (the list owns order / grouping / scroll / scale, never the item; *display* not *select*); 2 base shells (card list / row list) + a sectioning layer + a tree / drill-down form; owns the **row** (the one item with no meaning outside a list — 56pt, one trailing affordance); the headline **scale rule** — virtualize past the fold, update in place, **250 SP cards without lag (K-15)**, retiring v3's full-`innerHTML` re-render; doctrine-ordered (lifecycle / floor, never alphabetical); no silent removal (red-slash → `card.md`); **mints no tokens**; >90 v3 list / row / section / tree classes → one vocabulary; SR scripts registered in `accessibility.md`)
- `03-primitives/input.md` 🟢 (#189 — the data-entry primitive: **input-vs-picker as a rule** (*you type an input; you pick a picker* — v3's `<select>` → pickers, checkbox → `toggle.md`, 1×–4× qty → `segmented.md`); **7 variants** — text / text-area / numeric / measurement / search / chip-tag-input / multi-select filter chips; receives three handoffs — the **interactive chip** (`.app-chip`+`×`) from `badge.md`, the **measurement-display fraction component** from `typography.md`, and the **multi-select filter** (the v3 Gold/Grey/LockStroke `.system-toggle`) from `segmented.md`; measurement field = 56pt gloved keypad + 1/8″ stacked fractions (ADR-012, essay 07); validation moves v3 toast/`alert()` → inline `aria-invalid` + specific message, never color-alone; reversible edits never confirm (ADR-010); **mints no tokens** — cites `--radius-input` / `--accent` / `--danger` / `--type-mono` / `--motion-micro`; SR scripts registered in `accessibility.md`)
- `03-primitives/toggle.md` 🟢 (#190 — the binary switch: **toggle-vs-segmented** (the v3 `.system-toggle` Gold/Grey/LockStroke is *multi-select*, so neither a toggle nor a segmented → `input.md` filter chips) **+ toggle-vs-checkbox** (selection → `list.md` / completion → `nested-checklist.md`) as a rule; 2 variants — setting toggle (immediate + reversible, Principle 6) / form toggle (staged, commits with its form); **state carried by thumb position, never color alone** (Principle 9); off is the safe default (Principle 5); flip = `--motion-micro` slide+fill; **mints no tokens** — cites `--accent`/neutral/`--surface-card`/`--motion-micro`/56·48pt row; OS-native-switch fallback; SR script registered in `accessibility.md`)
- `03-primitives/segmented.md` 🟢 (#191 — the always-visible inline single-select: the deep spec of `picker.md`'s "Inline segmented" variant; **single-select-vs-everything as a rule** — vs the binary switch (→ `toggle.md`), the **multi-select filter** (→ `input.md`; the v3 Gold/Grey/LockStroke `.system-toggle` is multi-select per `getActiveSystemFilter()`, so *not* segmented — corrects the picker seed), the overlay picker (→ `picker.md`/`sheet.md` at 6+ options), the button group (→ `button.md`), the bottom nav (app shell, Phase F); **2 variants** — value (`radiogroup`; the v3 `.theme-toggle`) / scope-tabs (`tablist`; the v3 `.apparatus-tabs`); **mints no tokens** — cites `--radius-button` − `--space-1` = `--radius-input` (concentric indicator), surfaces/`--accent`/text, `--type-body-medium`, `--motion-micro`; selected = raised pill + weight bump + text-color step (sunlight → 2pt `--accent` border; broadcast → not rendered); **indicator changes in place, never a track-traversing slide** (motion budget); roving-tabindex + arrow-key nav closes the v3 per-segment-tab-stop gap; SR script registered in `accessibility.md`)
- `03-primitives/slider.md` 🟢 (#192 — the slide-to-commit control governing the **ADR-010 status advance**: **slider-vs-value/toggle/segmented as a rule** and **v4 ships no value-range slider** (measurement = 1/8″ keypad per ADR-012, capacity demoted, brightness = theme switch); 2 configs — advance (leading-edge→right) / step-back (trailing-edge→left, secondary, permanent not a timed undo); **commit-past-threshold + snap-back** is the ghost-tap defense, the drag is direct-manipulation; **status-only domain** — deploy (Assign Equipment) + return (terminal) are buttons→`warning-gate`, never slides; the travel reveals the **destination status hue** (matches the badge commit cross-fade); **mints no tokens** — cites `--radius-button`/`--radius-input`/`--accent`/`--status-*`/`--surface-*`/`--motion-micro`/`--motion-status` + 56·60pt; `card.md` owns placement / `slider.md` owns the control; the load-bearing *assistive-tech-cannot-slide* button-equivalent contract, SR script already in `accessibility.md`)
- `03-primitives/toast.md` 🟢 (#193 — the transient, self-dismissing message: **toast-vs-modal / WarningGate / sync-indicator as a rule** (transient *and* non-blocking, or it is a different primitive); **3 variants** — confirmation / notification / error (≤1 non-destructive action, never undo); governed by **ADR-010** — the timed-undo toast is *retired*, status reversibility is the card's Step-back, so v3's lone `showToastHTML` undo-link (its only `innerHTML` / XSS path) is **deleted not ported**; safety disclosures are the `WarningGate` and persistent sync is the indicator — neither is a toast; one at a time, debounced, no urgency theater; **mints no tokens**; SR script in `accessibility.md`)
- `03-primitives/empty-state.md` 🟢 (#194 — the calm zero-state surface: **empty-vs-loading-vs-error as a three-way rule** (settle-before-empty; a safety/rating omission defers to the warning card, never a neutral "no results"; the unfilled org slot → `card.md`); **4 variants by why-it's-empty** — first-run (one create action) / filtered (clear-filter; may escalate to the would-fit informational `RecommendationCard`) / upstream-blocked (points elsewhere) / all-clear (success, no CTA); centered void-stack — muted set-glyph `--icon-size-lg`/`xl` @40% + headline + reason + ≤1 button (Principle 4/7/11); **mints no tokens** — cites `--icon-size-*` / `--text-secondary` / `--space-*`, never `--danger`; retires v3's 7 nothing-here renderings; no appearance animation; SR script registered in `accessibility.md`)
- `03-primitives/loading-state.md` 🟢 (#195 — the honest wait: **loading-vs-sync-indicator** (the ambient sync dot is instant/non-blocking, never a spinner → `color.md`/`badge.md`) **+ loading-vs-empty** (not-yet ≠ nothing; the boundary lives in `aria-busy` → `empty-state.md`) **as a rule**; **4 treatments** — busy control (v3 `guardClick`/`.btn-loading`) / skeleton placeholder / determinate progress / inline spinner; **local-first makes a loader the exception** (most reads are instant, show nothing), static-first, determinate-over-indeterminate, failure resolves inline not via `alert()`; canonizes v3's two field rulings — feedback-on-every-tap (Surfside IP-010) + static-over-animated (`.btn-loading`'s removed pulse); **mints no tokens** — cites `--surface-card-hover` / `--surface-stroke` / `--space-*` / `--radius-*` / `--motion-*` / 56pt rows; the unowned continuous-loop timing (spinner rotation, shimmer) flagged for the slice; SR scripts registered in `accessibility.md`)
- `03-primitives/nested-checklist.md` 🟢 (#196 — the doctrine attestation tree; the one **net-new primitive** (no v3 antecedent — grep-confirmed; it *prevents* sprawl rather than retiring it): **leaf-vs-section as a rule** (only leaves are checkable; a section's state is *derived* — no one-tap bulk-attest of doctrine); backs the three D6 screens (IC Command / Task Level / ORM-TCRM) with one component; **every check is signed** (role spelled-out + mono time, visible + audited per D7.5); tap-to-toggle (reversible attestation) deliberately *not* the card's safety-slide; progress = tabular count, not an animated bar; collapse snaps, only the chevron animates; **mints no tokens** — cites `--icon-size-*` / `--icon-stroke-*` / `--space-4` indent / `--motion-micro` / 56pt row / `--accent`; reconciles D6's pre-token checkbox sizes (32pt sunlight → stroke-heavy, 48pt broadcast → `--icon-size-xl`); SR scripts registered in `accessibility.md`)
- `03-primitives/warning-gate.md` 🟢 ([#293](https://github.com/Vergo402/paratech-struts/issues/293) — the **14th cascade file**, added at the Phase E audit 2026-06-07 per matrix **K-11**: the **persistent safety disclosure** (one primitive, three uses — unrated zone, over-capacity, liability disclaimer); **distinct from `modal.md`** (which owns destructive/terminal/inventory *confirmation*) and `toast.md` (transient) — the disambiguation the cascade had conflated; rides the `RecommendationCard`, **never auto-dismisses** (Principle 7), the unrated zone gates Deploy behind an explicit acknowledgment; **mints no token** — cites `--danger` + the Warning glyph; SR script registered in `accessibility.md`)
- `03-primitives/side-drawer.md` 🟢 ([#306](https://github.com/Vergo402/paratech-struts/issues/306) — the **15th cascade file**, added at the Phase F #217 gate 2026-06-09 per **ADR-019**: the **edge-anchored slide-in companion panel** carrying the checklist side-tab (Task Level #204 + IC Command #203); **side-drawer-vs-sheet-vs-modal as a rule** (companion vs bottom-interrupt vs center-stop); one v4.0 variant — the checklist drawer (holds `nested-checklist`); phone = scrimmed overlay, tablet/laptop = companion beside a **live** canvas, broadcast = never; **mints one token — `--shadow-drawer`** (the sideways-inward cast, ADR-011 §Addendum), cites the rest; SR script registered in `accessibility.md`)

### Reference Teardowns (Phase B. Real product names, comparative analysis under nominative fair use, the legal doctrine that lets you name a competitor's product when comparing truthfully. See ADR-001.)

- `04-references/tablet-command.md` 🟢 (2339w)
- `04-references/first-due.md` 🟢 (2637w)
- `04-references/rednmx.md` 🟢 (2599w)
- `04-references/iamresponding.md` 🟢 (2498w)
- `04-references/rapidsos.md` 🟢 (2347w)
- `04-references/fire-rescue-systems.md` 🟢 (2435w)
- `04-references/positioning.md` 🟢 (2613w synthesis, 2 axis chart: tactical vs records by doctrine fluent vs agnostic)
- `04-references/nims-org-structure.md` 🟢 (Phase D follow-up — NIMS Group/Division/Supervisor doctrine + v3→NIMS mapping; all 7 questions resolved 2026-05-31 → ADR-008)
- `04-references/database-evaluation.md` 🟢 (Phase D follow-up — backend evaluation; stay on Firebase RTDB for v4.0 behind a `data/sync` seam → ADR-009)

### Brainstorm Essays (Phase C. Target was 2,750–3,000 words each, 250 word exec summary + numbered recs. Several came in over target where the lens earned it; no padding flagged.)

- `05-essays/01-architecture.md` 🟢 (5522w, 30 recs)
- `05-essays/02-visual-language.md` 🟢 (5665w, 18 recs)
- `05-essays/03-ic-workflow.md` 🟢 (3787w, 14 recs)
- `05-essays/04-future-scale.md` 🟢 (3617w, 12 recs)
- `05-essays/05-nims-doctrine.md` 🟢 (4417w, 22 recs)
- `05-essays/06-domain-ux.md` 🟢 (4088w, 25 recs)
- `05-essays/07-field-conditions.md` 🟢 (4610w, 18 recs)
- `05-essays/08-skeptical-review.md` 🟢 (3204w, 15 recs)
- `05-essays/09-data-resilience.md` 🟢 (6779w, 20 recs)
- `05-essays/10-implementation.md` 🟢 (5732w, 25 recs)
- `05-essays/11-scenario-stress.md` 🟢 (6026w, 18 recs)
- `05-essays/12-tech-debt.md` 🟢 (6112w, 30 recs)

### Synthesis (Phase D)

- `06-synthesis.md` 🟢 (12 convergent themes, 7 productive conflicts, 4 surprises, recommended path; Alex's PR #282 review folded in; held sections resolved via ADR-008/ADR-009)
- `06-decision-tracking-matrix.md` 🟢 (247 recs from all 12 essays — 218 accepted / 13 deferred / 15 rejected / 1 merged after PR #282 review)

### Design System (Phase E)

- `07-design-system/color.md` 🟢 (gate trio — APPROVED 2026-06-01; 4 themes light/dark/sunlight/broadcast, reconciled status palette, all ratios verified)
- `07-design-system/typography.md` 🟢 (gate trio — APPROVED 2026-06-01; Geist + Geist Mono ramp, tabular numerals, 1/8″ digit-pair fractions)
- `07-design-system/wcag-contrast.mjs` 🟢 (reproducible WCAG verification — `node …/wcag-contrast.mjs`)
- `preview/` 🟢 (**living styleguide** — token-driven static HTML rendering the gate trio in all 4 themes; `npx serve docs/v4-design/preview` or the `v4-styleguide` preview config. A mockup, not the build; `preview/tokens.css` is reused in Phase H)
- `07-design-system/spacing-grid.md` 🟢 (E2 — 4pt internal grid / 8pt external rhythm, tokens `--space-1`–`--space-12`; complete 5-value corner-radius vocabulary; touch-target table reconciling B-16/G-1/G-7/G-17/B-6; surface breakpoints phone/tablet/laptop/broadcast; elevation rule)
- `07-design-system/motion.md` 🟢 (E3 — six duration tokens `--motion-*` + five easing tokens `--ease-*`; three legitimate animation jobs; what-moves/what-doesn't; **drops the 5s/8s timed-undo line per ADR-010**, status commit = slide-to-advance cross-fade + passive red-slash; haptics pair with motion; broadcast = zero motion; `prefers-reduced-motion` → instant swap. Tokens synced to `preview/tokens.css`.)
- `07-design-system/iconography.md` 🟢 (E4 — four-size artboard grid `--icon-size-sm/md/lg/xl`; `--icon-stroke-default` 1.5px + `--icon-stroke-heavy` 2px sunlight override; `--icon-radius` 2px derived; outlined/filled states via `--motion-micro`; ~46-glyph inventory across 6 categories; shore type diagrams two-artboard treatment + doctrine accuracy requirement; color from existing tokens, no new icon-specific tokens. Tokens synced to `preview/tokens.css`.)
- `07-design-system/voice-and-tone.md` 🟢 (E5 — copy doctrine: the creed *terse · doctrine-aligned · never cute*; **ratifies the seven status display labels** `color.md` deferred here, incl. the renames **Strut Set** / **Shore Secured** / **Strut Equipment Returned** with rationale; how-the-app-talks before/after rules (buttons, off-queue red-slash, warnings/disclaimers, empty states, toasts, sync); **NIMS terminology lock** via the "Chief Test" (titles spelled out, no acronyms; do-not-say/do-say table); per-surface + accessibility + anti-patterns. **Mints no CSS tokens** — copy is prose. Doc-only, no ADR; cites ADR-008/010/011/012 + matrix E-14.)
- `07-design-system/logo-and-mark.md` 🟢 (E6 — **new identity** per #10/ADR-013, not a refresh: a **full-color FS emblem** — gold LongShore vertical + 2 aluminum struts, rigid 6″ base/sole plates both ends; S of cut 4×4 + plywood gussets, 9-nail — paired with a **simplified mono mark** (single ink, themes via `--accent`, 16px-safe). App-icon, favicon, wordmark + emblem in `assets/logo/`. [`ADR-013`](11-decisions/ADR-013-brand-emblem-full-color.md) exempts the emblem from the one-accent rule. Rendered proof: Logo panel in `preview/`.)
- `07-design-system/accessibility.md` 🟢 (E7 — authored last, the consolidation point: **WCAG 2.1 AA floor / AAA where reachable** (sunlight + broadcast 7:1); consolidates the a11y sections of all six sibling docs *by reference, not restatement*; the load-bearing rule **"assistive tech cannot slide"** — the slide-to-advance commit always has focusable, labeled **Advance / Step-back** button equivalents + `aria-live` announce, plus the **Power Select** native-`<select>` fallback on VoiceOver/TalkBack-or-Settings; a screen-reader **script registry** (*Role · Name · State · Action-hint* grammar + concrete scripts for the 2 filled primitives + global patterns; #183–196 extend it); modal focus-trap / skip-link / keyboard parity; haptics survive reduced-motion. **Mints no CSS tokens.** Lean baseline per Alex; formal SC-by-SC audit deferred to Phase H/J.)
- `LESSONS.md` 🟢 (recs L-3 / L-23 — institutional memory of v3 audit-driven fixes: load-table conservative floor, deduct-once, the `escapeHtml`-vs-`escapeAttr` XSS class, local-first writes, the v3.8.2 silent-validation-failure, listener teardown, `STATUS_ORDER`, transactions, iOS/SRI, IDs. 10 lessons, extensible. Written 2026-06-07 by the A–E traceability audit.)
- `07-design-system/architecture.md` 🟢 (rec A-30 — the whole-system **map** at picker depth: single-package folder tree, two entry points, Vite build, the one-tap data-flow story (commit → IndexedDB + event append → sync queue → merge-by-log), the stack as a coherent whole, the 800-line ceiling + CI boundary check as discipline; cites ADR-005/007/023/024/009, mints no tokens; #310, resolves OQ #24)
- `07-design-system/module-boundaries.md` 🟢 (rec L-1 — the seam-by-seam **constitution**: one section per the canonical 11 seams (Purpose · import contract · exports · the v3 LESSON it carries · anti-patterns · ADRs), the three structural invariants, the retire-from-v3 + crosses-verbatim tables, all 10 LESSONS pinned to seams (L-3 = JSX/lint, not a seam); #310, resolves OQ #25)

### Information Architecture (Phase F)

Per screen specs across all four surfaces (phone / tablet / laptop / broadcast TV). One file per screen including v3 derived screens, three checklist screens (D6), and the auth/dept/admin screens (D7).

**Foundation (2026-06-07, mini-gate PASSED — Alex):**
- `08-information-architecture/00-ia-foundation.md` 🟢 (the "picker.md of Phase F": 5-tab spine + full tab map, nav across 4 surfaces + guest-first routing, modal-vs-sheet per-screen table, four-surface density framework, persistent-chrome contract, empty/error/loading posture, screen inventory + sequencing. Anchors ADR-014/015/016.)
- `08-information-architecture/_TEMPLATE.md` 🟢 (per-screen IA skeleton at picker/card depth — composed-primitives + locked-rules checklist + 4-surface table + cite-don't-restate a11y + per-screen OQs)
- `11-decisions/ADR-014-tab-structure.md` 🟢 Accepted (keep the v3 5-tab spine; nest, don't expand — resolved #214)
- `11-decisions/ADR-015-navigation-pattern.md` 🟢 Accepted (fixed bottom nav, push drilldown, progressive across surfaces, guest-first cold-open — resolved #215)
- `11-decisions/ADR-016-modal-vs-sheet-rules.md` 🟢 Accepted (ratify the sheet/modal doctrine, apply per-screen — resolved #216)
- `11-decisions/ADR-017-custom-department-roles.md` 🟢 Accepted (Admin = the only built-in role + editable Default + dept-defined custom roles with ~8 back-office permission toggles; back-office only — fireground stays ICS-position-gated; data-driven rules — #304 / #217 gate)
- `11-decisions/ADR-018-after-action-auto-email.md` 🟢 Accepted (a **Principle-10 scope clarification, NOT an exception**: an after-action record read *after* an incident closes is outside the radio rule — documentation, not communication; the record auto-emails to IC/Ops on incident-complete, on-by-default + dept-disableable, records-only / never during an op / never tactical — #305 / #217 gate)
- `11-decisions/ADR-019-side-drawer-primitive.md` 🟢 Accepted (the **15th UI primitive** — a gate escalation: the edge-anchored side-drawer **companion** panel carries the two checklist screens; mints one token `--shadow-drawer` (ADR-011 §Addendum); re-homes #203 / #204 — #306 / #217 gate)
- `11-decisions/ADR-020-department-checklist-authoring.md` 🟢 Accepted (Phase G [#230](https://github.com/Vergo402/paratech-struts/issues/230) — a **Principle-1 scope clarification** extending ADR-002: **departments fully author their own checklists** — FieldShore ships a paraphrase-approved **baseline**, departments **fork + tailor** (add/edit/remove/reorder + reset-to-baseline); a dept authoring its own SOPs is *configuration, not the app inventing doctrine*; guardrail = **provenance** (baseline vs. "Customized by \<dept\>"), not restricting authoring; **Admin-only**, dept owns outright (no FieldShore review), audited; safety engine never a template; constitution not edited inline — Alex, 2026-06-09)
- `11-decisions/ADR-021-command-transfer-handshake.md` 🟢 Accepted (Phase G gate **B1** — command transfer is a **two-party handshake**; the **outgoing IC retains command until the incoming accepts**, so the incident is **never in a no-IC state** and the IC-gated End-Op is always reachable. Alex chose "requires accept"; the retain-until-accept invariant removes its only failure mode (an offline incoming IC stranding the op). The verbal "you have command / I have command" stays on the radio; the app records both ends (initiated-by → accepted-by, timestamped); no push — the incoming IC sees a **pending-acceptance** state on next sync — Alex, 2026-06-09)
- `11-decisions/ADR-022-mutual-aid-v40-qr-guest.md` 🟢 Accepted (Phase G gate — **mutual aid moves v4.5 → v4.0**, superseding the ADR-003 *deferral* (scope unchanged: local 2–5 depts, not federal/IST). Three coupled calls: (1) **cross-dept incident sharing ships v4.0** incl. the **merged multi-agency audit roll-up**; (2) join = **QR primary + human-readable code fallback**, multi-use·time-boxed·revocable·auto-expires, **castable to the C-13 board**, QR everywhere in v4.0 (fixes gate B4 — the one-time 24h code retired); (3) **guest participation** — an un-provisioned device scans/enters → works the incident as a **guest** (per-device anon UID, **typed unit tag**, **v3 external-equipment handling — no on-the-fly deployable inventory**, read-only/claimable on close, "Guest · «unit»" in the audit). **Expands the v4.0 slice** (master plan flagged); the camera scanner is the lone new platform surface (99-OQ #42) — Alex, 2026-06-09)

**Phase H foundation (build-stack ADRs — 2026-06-09, all 🟢 Accepted — foundation mini-gate PASSED Alex 2026-06-10; ratify the Phase-D-gate stack):**
- `11-decisions/ADR-005-single-package-pwa.md` 🟢 Accepted (**single-package v4.0 PWA**, two entry points `src/app/` + `src/site/`, one Vite config; Turborepo monorepo + React Native = the v5 fork; names the 11 module seams + the `data/` boundary verbatim with ADR-007; resolves OQ #8; closes [#240](https://github.com/Vergo402/paratech-struts/issues/240))
- `11-decisions/ADR-006-reserved-schema-fields.md` 🟢 Accepted (**reserve v5's namespace on every record, enforce none in v4**: `agencyId`, IC-as-collection, append-only `roleHistory`, `arrivedAt`/`demobbedAt`, `nets[]`, `resourceType`, inventory `status` enum, `linkedVictim`; rides the ADR-009 event-log append; essay 04 recs 1–11; closes [#309](https://github.com/Vergo402/paratech-struts/issues/309))
- `11-decisions/ADR-007-build-system-typescript-strict.md` 🟢 Accepted (**Vite + TypeScript strict** from line one; retires the v3 no-build single file; ~30% of audit findings are the compile-time-catchable class; 800-line file ceiling + the same 11 seams as ADR-005; resolves OQ #11/#12; closes [#242](https://github.com/Vergo402/paratech-struts/issues/242) + [#244](https://github.com/Vergo402/paratech-struts/issues/244))
- `11-decisions/ADR-023-component-state-stack.md` 🟢 Accepted (**Radix headless + Tailwind v4 + TanStack Router + TanStack Query + Zustand + Zod**; tokens drive appearance / Radix drives behavior; Shadcn (style lock-in) + Material (brand mismatch) rejected; fills the `ui/*` seams; resolves OQ #13; closes [#243](https://github.com/Vergo402/paratech-struts/issues/243))
- `11-decisions/ADR-024-d5-multi-device-build-a.md` 🟢 Accepted (**D5: Build A (accept-and-reconcile) default in v4.0; Build C (CP hub) → v5/RN** — no v4.0 Build-choice control (Principle 11), surfaces in v5 when real; per-device anon UID → IndexedDB/Dexie, per-row sync on Accountability; formalizes ADR-009's deferred ordering; refines the older v4.5 guess to v5; resolves OQ #7; closes [#241](https://github.com/Vergo402/paratech-struts/issues/241))
- `11-decisions/ADR-025-authentication-implementation.md` 🟢 Accepted (**D7.1: Firebase Auth email+password + email magic-link**; mandatory display name; offline auth window queues the sign-in intent; the guest path stays [ADR-022](11-decisions/ADR-022-mutual-aid-v40-qr-guest.md)'s — referenced, not redefined; no SSO/2FA in v4.0; resolves OQ #4b; closes [#245](https://github.com/Vergo402/paratech-struts/issues/245))

**Screen specs:**
- `08-information-architecture/20-operations.md` 🟢 (#199, Session 1 — the live shoring job board: 7 status lanes with the v4 labels, the `ShorePointCard` per status with slide-to-advance, the **K-5 card-tear fix** (one SP card per point; fitting alternatives live only in the Assign-Equipment sheet), drilldown building→division→area with `group`→**assignedResource**, the grouped-shore phase-split, role gates, 4 surfaces)
- `08-information-architecture/21-cutting-station.md` 🟢 (#294, Session 1 — the cut workstation under Operations: the v3 Cut Table promoted to a first-class screen; FIFO queue + tablet drag priority (phone read-only); cut length the one big number; Mark Cut Done / Send to Runner as slide commits; off-queue red-slash; 4 surfaces)
- `08-information-architecture/10-quick-find.md` 🟢 (#198, Session 2 — the standalone strut calculator + the **cold-open guest landing**: input flow (measurement / deduction visual-grid picker sheet / load / system **multi-select filter chips**), results = `RecommendationCard` stack (capacity demoted, WarningGate), the v3 hold-to-start **FAB retired → a labeled Start-Operation button**, the would-fit informational empty state; 4 surfaces)
- `08-information-architecture/40-inventory.md` 🟢 (#200, Session 2 — apparatus-centric stock: apparatus scope segmented, equipment list (system→type, ± qty stepper, deployed badge), Add Equipment / Add Apparatus per ADR-016 + preserved visual-grid plate picker, Quick View, Excel import/export with ID round-trip + orphan-confirm + determinate loader; **per-row sync deferred to Accountability #297**; 4 surfaces)
- `08-information-architecture/30-command-sitstat.md` 🟢 (#201, Session 3 — the IC home: **SitStat = the Command home composition** (six canonical datums), one-tap Org Chart + Hazard Log, **command transfer** (single-action → full-screen takeover) + role history, role-assignment sheet + resource roster, End-Op modal; Command-vs-Accountability boundary; broadcast = the C-13 board; 4 surfaces)
- `08-information-architecture/31-org-chart.md` 🟢 (#295, Session 3 — the ICS structure as a sheet/modal from SitStat: the **NIMS two-Group default** (Rescue + Shoring Group Supervisors; Entry/Wood/Runner tasks beneath; Cutting Station out), titles spelled out, the **K-12 ≤7×2 tablet-portrait budget** + tap-to-descend, IC-only edit, reparent drag/button, **role history one-tap (new in v4)**, span-of-control; 4 surfaces)
- `08-information-architecture/32-hazard-log.md` 🟢 (#296, Session 3 — the ICS-208 hazard register, one tap from SitStat / the Safety-Officer header: Add Hazard sheet (any role), open-first by severity, **ICS-208 export (new)** + **hazard badges feeding SP cards (new)**, **no safety-hold** — visible never blocking; 4 surfaces)
- `08-information-architecture/33-ic-command-checklist.md` 🟢 (#203, Session 4 — the IC's doctrine attestation tree under Command, one tap from SitStat: a deep 3–4-level phase tree composing `nested-checklist`; **phone-floor + tablet-canonical**; **tap-to-attest, never slide**; every check signed (D7.5); **auto-collapse ON**; **ICS-201 briefing structure ships v4.0, doctrine content v4.1**; 4 surfaces)
- `08-information-architecture/22-task-level-checklist.md` 🟢 (#204, Session 4 — the team officer's per-task attestation list under Operations: a 2-level tree composing `nested-checklist`; phone-floor; **one section open at a time**; tap-to-attest (contrast the shore-point status slide); attach-target (operation/task/SP/group) flagged as an OQ; content v4.1; 4 surfaces)
- `08-information-architecture/23-orm-tcrm.md` 🟢 (#205, Session 4 — the Rescue Group Supervisor's pre-task crew briefing: **a button-bar entry → full-screen-form modal/route, not a tree screen**; the TCRM 4 steps (Explain·List·Ask·Speak) + 5 questions composing `nested-checklist`; **NOT a safety-hold** (Principle 10); Begin/End-briefing buttons (session logic = Phase G); numeric ORM score out-of-scope OQ; auto-collapse OFF; 4 surfaces)
- `08-information-architecture/41-accountability.md` 🟢 (#297, Session 5; **renamed Roster → Accountability at the #217 gate**) — the accountability + sync lens under Inventory: the v3 Command rosters (apparatus / external / individuals) re-homed with **per-row sync state** (rec I-7, the PAR test case — **synced/pending only on the face, freshness on tap; no staleness greying** per #217); **Command owns assignment, Accountability owns accountability+sync**; external/mutual-aid return-to-source resolved here; **PAR made visible, never alarmed** (Principle 10); per-row sync-detail sheet, no destructive overlay; 4 surfaces)
- `08-information-architecture/50-settings.md` 🟢 (#202, Session 5 — the config home: v3 carry (themes, dept connection, data management, feedback, app updates, apparatus types, log out) + v4 new — **Native Controls** picker fallback, dept registration → guest-first auth routes, role-gated admin gateways (User Manager / Audit Log); **48pt non-operational targets**; toggles commit in place, log-out / leave-dept = destructive modal; resolves apparatus-type editing → here; 4 surfaces)
- `08-information-architecture/70-login-register.md` 🟢 (#206, Session 6 — the pre-shell sign-in / create-account route: **guest-first, never a cold-open gate** (Continue-as-guest first-class); email+password + magic-link (D7.1, tracked 99-OQ #4b); Sign In↔Create segmented; **net-new** (v3 = anonymous auth only); ships v4.0; 48pt non-operational; no broadcast; calm inline errors)
- `08-information-architecture/71-dept-setup.md` 🟢 (#207, Session 6; **#304/ADR-017: creator → first Admin**) — the pre-shell create-department route: one-time idempotent founding-Admin claim; one field (dept name) for <2-min zero-config; generates the first invite code; deferrable, never a gate; ships v4.0; founding-Admin transfer deferred v4.1+)
- `08-information-architecture/72-invite-code.md` 🟢 (#208, Session 6; **#304/ADR-017: enter code → the Default role**) — the pre-shell join-by-code route; **distinct from the cross-dept *incident* invite (#210)** (both v4.0 per ADR-022; that one scopes a unit to one incident, this joins a person to a department); paste convenience; calm invalid/expired/used errors; deferrable, never a gate; ships v4.0)
- `08-information-architecture/51-user-manager.md` 🟢 (#209, Session 7; **rewritten for custom roles — #304 / ADR-017**) — the Admin surface managing **Roles** (Admin built-in + editable Default + dept-defined custom roles, each a set of ~8 back-office permission **toggle**s) **and Members** (assign role, promote to Admin, revoke); **Admin = the only mandated role**, ≥ 1 always (anti-lockout); **back-office only — fireground actions stay ICS-position-gated**; assign/edit-role/promote = sheet, revoke/delete-role = destructive modal; **device-roles ≠ NIMS positions ≠ fireground gates**; net-new; model + rules ship v4.0, UI flagged (99-OQ #32); 2FA hook (99-OQ #33); 48pt, no broadcast)
- `08-information-architecture/52-cross-dept-invite.md` 🟢 (#210, Session 7; **rewritten to v4.0 — [ADR-022](https://github.com/Vergo402/paratech-struts/blob/v4-redesign/docs/v4-design/11-decisions/ADR-022-mutual-aid-v40-qr-guest.md)**, #210 reopened) — local mutual-aid incident sharing, **v4.0**: host IC/Admin generates a **per-incident QR + code** (multi-use·revocable·castable to the board); assisting dept → **scoped Member**, walk-up **guest** (typed unit tag, v3 external-equipment handling) → **scoped Guest on that one incident**; merged multi-agency roll-up v4.0; **distinct from the dept-level Invite Code #208** + v3 unscoped external equipment; generate/enter = sheet, revoke = destructive modal; not comms (permission grant); 48pt, broadcast renders only the cast QR image)
- `08-information-architecture/53-audit-log.md` 🟢 (#211, Session 7 — **the event-log read projection** (ADR-009, *audit == event log*); **After-Action folds in as the laptop surface**; the **export convergence point** (ICS-201/203/207/208/209 assembly + Accountability PAR snapshot + Hazard ICS-208); inline scope segmented + filter, **no destructive overlay** (immutable); **read + export = IC/Operations only (#217)**; **after-action auto-email to IC/Ops on incident-complete = outside the radio rule's scope, a Principle-10 scope clarification ([ADR-018](11-decisions/ADR-018-after-action-auto-email.md), #305)**; event-log persistence v4.0, UI flagged (99-OQ #32); 48pt, no broadcast)
- `08-information-architecture/60-broadcast-view.md` 🟢 (#213, Session 8 — **the read-only projection MODE**, not a tab screen: the **projection registry** over all 18 screens (full board: Operations / SitStat-C-13 / Hazard / Accountability · reduced: Cutting Station / Org Chart / Inventory · minimal: the 3 checklists · **not rendered**: Quick Find + the admin + auth clusters); **default = SitStat C-13**; **pin-not-mirror** (the wall board never follows the caster's taps); output renders only read-only badge / card / list (**no interactive primitive** — ADR-016), ≥ 32pt, zero motion, color-never-alone (border+label), no comms; cast control honors phone-floor; cast transport → 99-OQ #34. **The final Phase F screen spec.**)

**Per-screen specs (remaining):** **none — all 19 screen specs complete** (Session 8 shipped Broadcast View [#213](https://github.com/Vergo402/paratech-struts/issues/213); the full Phase F gate is [#217](https://github.com/Vergo402/paratech-struts/issues/217)). *Not created:* Demo Mode (#212, closed) · Activity Feed (Principle 10) · SitStat (folds into #201) · After-Action (folds into #211).

### Workflows (Phase G)

Per workflow specs with state diagrams, screen by screen wireframes, interaction notes, undo windows, accessibility scripts. Cross surface story for each.

**Foundation (2026-06-09, mini-gate APPROVED — Alex, 2026-06-09):**
- `09-workflows/00-workflow-foundation.md` 🟢 (the "picker.md of Phase G": workflow-vs-screen boundary + litmus test; **Mermaid `stateDiagram-v2`** locked as the state-diagram notation w/ the `actor · gesture → primitive` transition grammar; the text-wireframe frame; the **cross-surface actor-surface matrix** — Phase G's distinct job, propagation via the event log on sync not a push; reversibility cited from [ADR-010](11-decisions/ADR-010-status-commit-model.md) (timed-undo retired); accessibility-reuse; cite-vs-own; file-naming bands. **Mints no tokens**, doc-only — no ADR, like `voice-and-tone.md`.)
- `09-workflows/_TEMPLATE.md` 🟢 (per-workflow skeleton at foundation depth — Purpose/goal · Actors & surfaces · State diagram · Step-by-step (wireframe + commit + reverse + app response) · Cross-surface story matrix · composed screens+primitives · locked rules · empty/error/loading · a11y script extensions · per-workflow OQs)

**Workflow specs:**
- `09-workflows/05-setting-up-inventory.md` 🟢 ([#218](https://github.com/Vergo402/paratech-struts/issues/218), **the Phase G worked example**) — the verb of [`40-inventory.md`](08-information-architecture/40-inventory.md): 2 state diagrams (stock-setup lifecycle + the 4-step import sub-flow w/ orphan guard), step wireframes for **Add Apparatus** / **Add Equipment** (quick-add grids + preserved visual-grid plate picker) / **Bulk import** (Flatfile-style 4 steps → orphan-detection modal → determinate loader); single-actor cross-surface story (one role across devices, phone-floor import is real); reversibility = ± clamp / remove-at-0 / destructive Delete-Apparatus modal; **no status slide** (a setup flow — stated, not skipped); registers one new SR script (the import step-indicator)
- `09-workflows/10-starting-an-operation.md` 🟢 ([#219](https://github.com/Vergo402/paratech-struts/issues/219), Session 2) — the verb of the Operations empty-state: state diagram (NoActiveOperation → StartModal → ActiveOperation; edit reverse; terminal End Op arc feeding #238); 3-step happy path (empty-state → full-screen-form modal → active-op board); persistent chrome header updates on commit; **single-actor story** (IC's phone drives, any connected device reflects on next sync); reversibility = edit modal (permanent, no timed undo per ADR-010); End Operation = terminal destructive gate. No new SR scripts.
- `09-workflows/11-adding-a-shore-point.md` 🟢 ([#220](https://github.com/Vergo402/paratech-struts/issues/220), Session 2) — the verb of the active-operation board's Add Shore Point action: state diagram (ActiveOperation → AddSPModal → PendingSP; edit/delete reverse while Pending; SP exits workflow on first advance to #221); 3-step happy path (ops board → full-screen-form modal → Pending lane); **grouped SP creation** (T-Shore / Double-T: N linked Pending cards, group badge N/total, pre-cutting advances group-wide); measurement input + deduction picker **reused from Quick Find by reference** (same component, `10-quick-find.md` cited, not redrawn); **two-actor story** (team officer phone + IC tablet CP); field-lock post-Pending (shore type + measurement lock; label remains editable). No new SR scripts.
- `09-workflows/12-deploying-a-strut.md` 🟢 ([#221](https://github.com/Vergo402/paratech-struts/issues/221), Session 3) — the Assign Equipment arc: state diagram (PendingSP → AssignSheet → WarningGate path or InProcess direct; step-back from InProcess = inventory-consequential modal; InProcess → StrutSet → exits to #222); Assign Equipment = bottom-anchored picker sheet (60vh) showing `RecommendationCard` stack ranked by fit; gated deploy intercepts unrated/over-capacity inline without dismissing the sheet; **deployed-strut cradle-to-grave** begins at InProcess commit (model + apparatus source persists through Strut Equipment Returned); grouped SP: each member deploys independently, group-wide pre-cutting advance triggers once ALL members leave Pending; inventory decrements local-first.
- `09-workflows/13-cutting.md` 🟢 ([#222](https://github.com/Vergo402/paratech-struts/issues/222), Session 3) — the two-surface cutting arc: **Operations board arc** (Strut Set → Cutting, group-wide slide, last group-wide advance in the lifecycle) + **Cutting Station arc** (Mark Cut Done → Send to Runner, individual per card); `CutDone` is an internal flag on `cutting` state — not a separate Operations lane; cut length is the **one promoted number** (bold, cutting-hue); optional actual-cut override; red-slash "Removed from cut list" on step-back out of queue — never silently removed; priority reorder by drag = tablet only; Cutting role gate + IC/Safety override.
- `09-workflows/14-runner.md` 🟢 ([#223](https://github.com/Vergo402/paratech-struts/issues/223), Session 3) — the simplest workflow in the arc: one slide (Runner → Shore Secured), individual; step-back to Cutting preserves `cuttingDone` flag (saw already ran — step-back is about runner not ready, not re-cutting); Runner role gate + IC/Safety override. No confirm at any step.
- `09-workflows/15-secured-returned.md` 🟢 ([#224](https://github.com/Vergo402/paratech-struts/issues/224), Session 3) — the final two states of the SP lifecycle: **Shore Secured** (runner confirms strut set; slide, no confirm, role-gated Runner; step-back available) + **Strut Equipment Returned** (officer retrieves equipment at operation end; inventory-consequential confirm modal — "This cannot be undone" — terminal, no step-back); two distinct moments often hours apart; terminal state: card stays read-only until operation archives (#238); inventory increments local-first on submitting device.
- `09-workflows/16-end-of-operation.md` 🟢 ([#238](https://github.com/Vergo402/paratech-struts/issues/238), Session 4) — the lifecycle's **lone terminal action**: state diagram (ActiveOperation → EndConfirmModal → ArchivedOperation, terminal; auto after-action delivery side effect); IC-only destructive confirm modal (default-Cancel, "final and cannot be re-opened", no timed undo per ADR-010); board reverts to empty-state, op drops to read-only Past operations, cards freeze in place (no forced advance to Returned); **after-action auto-email** per ADR-018 (records-only · on incident-complete · IC/Ops/Admin fallback · export-convergence packet · on-by-default + dept-disableable · never during op, never a push); single-actor (IC) story. OQs: archive storage/access, re-open, still-deployed warning, email transport (all Phase H).
- `09-workflows/20-role-assignment-command-transfer.md` 🟢 ([#225](https://github.com/Vergo402/paratech-struts/issues/225), Session 4) — the two Command verbs: **role assignment** (My Role sheet — any device picks its ICS position; IC assigns resources to org-chart nodes via sheet; reparent by tablet-drag / AT-button-equivalents; span-of-control = informational badge never a block; destructive removal of a *populated* position = modal) + **command transfer** (full-screen takeover, NOT a stacked modal per ADR-016; writes an append-only **role-history** event — new in v4; two-device accept; gold accent follows the new IC); **two orthogonal axes** (ADR-017 back-office role ≠ ICS position); NIMS two-Group default + titles spelled out (ADR-008). OQs: transfer handshake, briefing default by Level, My-Role/org-chart collision (Phase H).
- `09-workflows/21-hazard-log.md` 🟢 ([#226](https://github.com/Vergo402/paratech-struts/issues/226), Session 4) — the ICS-208 register, **visible to everyone, blocking to no one**: Add Hazard sheet (any role; type/location/severity-badge-with-text/notes); open-first-by-severity register, mitigated drops to bottom; **mitigate ↔ reopen** reversible, no destructive path in the common flow; **SP-card hazard badge** (new in v4 — a hazarded area surfaces a badge on shore-point cards, the **advance slide still works** — the concrete no-safety-hold rule, Principle 10); **ICS-208 export** (new, format shared with #238 after-action); no `safety-hold` status anywhere. OQs: hazard↔area binding precision, badge-clear timing, export format, mitigate gate (Phase H).
- `09-workflows/22-ic-command-checklist.md` 🟢 ([#227](https://github.com/Vergo402/paratech-struts/issues/227), Session 4) — the IC's deep doctrine attestation tree in a **summonable side-drawer companion** (ADR-019): the drawer commits nothing (container); inside, **tap a leaf to attest** (never the safety-slide — ADR-010), every check **signed** (role spelled out + mono time, e.g. "Incident Commander · 14:32", audited D7.5); section state **derived** (no one-tap "complete the phase"); **auto-collapse ON** for the deep tree, active branch never auto-collapses (Principle 7); progress = count not bar, no completion celebration; un-check reversible + audited, no confirm; **ICS-201 briefing card** (structure v4.0 / content v4.1, the payload #225's transfer attaches); phone-floor + tablet-canonical companion (no scrim, beside live SitStat); broadcast renders headers+counts, no toggle.
- `09-workflows/23-task-level-checklist.md` 🟢 ([#228](https://github.com/Vergo402/paratech-struts/issues/228), Session 4) — the team officer's shallow 2-level task tree in a **side-drawer companion** on the active-operation screen (ADR-019): **tap a leaf to attest** with the **deliberate tap-here/slide-there contrast** spelled out (tap = reversible doctrine attestation; the shore-point status slide = safety-consequential — same screen, never confused); **one section open at a time** (phone default), auto-collapse-completed off; signed checks (D7.5), derived section state, count not bar, no celebration; un-check reversible no confirm; tablet companion lets Ops Section Chief read the tree beside the resource board. OQ: **attach-target** (operation/task/SP/group — working assumption: task scoped to the active op; resolved Phase H/I).
- `09-workflows/06-signing-in-and-out.md` 🟢 ([#234](https://github.com/Vergo402/paratech-struts/issues/234), Session 5) — the per-device auth verb, **always forward, never a gate** (ADR-015 guest-first cold-open): reached from a dismissible "Sign in to sync" banner / Settings; Sign In ↔ Create Account segmented, email+password + magic-link, **Continue-as-guest first-class**; the **offline auth window** = queue the sign-in intent locally + tell the user plainly, completes on reconnect (local-first, ADR-009 — never stranded at a login screen); sign-out → guest, **local work persists**; net-new (v3 = anonymous auth only); no destructive path; 48pt, no broadcast. OQs: auth-method confirm (#4b), offline token lifetime, display-name capture point, migration UI.
- `09-workflows/07-department-setup.md` 🟢 ([#231](https://github.com/Vergo402/paratech-struts/issues/231), Session 5) — found a department in <2 min: **one field** (department name) → **Create** → creator becomes the **first Admin** via an **idempotent write-once claim** (only succeeds if no Admin yet — race-safe; ADR-017) → success sheet surfaces the **first invite code** (one-time, 24h); the post-v3-migration "Claim department" banner lands at the same created state; deferrable (Continue-as-guest); net-new; additive, no destructive path; founding-Admin transfer deferred v4.1+. OQs: metadata beyond name, code surfacing, multi-dept membership.
- `09-workflows/08-joining-by-invite-code.md` 🟢 ([#232](https://github.com/Vergo402/paratech-struts/issues/232), Session 5) — join via **one field** (invite code, paste convenience) → land as a **Default-role** member (read everything + run field work; Admin can reassign later); calm inline invalid/expired/used errors (never alert); **distinct from the cross-dept *incident* invite (#210)** (both v4.0 per ADR-022); **dept code is now multi-use + QR** (gate B4 fix, ADR-022); deferrable; offline join queues; net-new; no destructive path (leaving = a Settings action). OQs: code format/alphabet, display-name point, multi-dept membership.
- `09-workflows/24-tcrm-briefing.md` 🟢 ([#229](https://github.com/Vergo402/paratech-struts/issues/229), Session 5) — the Rescue Group Supervisor's pre-task crew briefing: button-bar **Begin briefing** → full-screen-form modal (stamps start) → walk the **four steps (Explain·List·Ask·Speak) + five crew questions** (shallow `nested-checklist`, auto-collapse OFF, tap-to-attest, signed) → **End briefing** (stamps end, records session); **why no countdown timer** stated as a design rule (a timer would make it a gate — violates Principle 10); **NOT a safety-hold** — never blocks crew entry/deploy/advance, the "Speak" step *names* radio/face-to-face stop authority, doesn't implement it; net-new; surface+primitive ship v4.0, wording v4.1. OQs: session record shape + attach-target, numeric ORM score out-of-scope, content v4.1.
- `09-workflows/25-customizing-checklists.md` 🟢 ([#230](https://github.com/Vergo402/paratech-struts/issues/230), Session 5; **rewritten 2026-06-09 per [ADR-020](https://github.com/Vergo402/paratech-struts/blob/v4-redesign/docs/v4-design/11-decisions/ADR-020-department-checklist-authoring.md)** — Alex: *"100% they should be able to… provide a generalized flow, departments customize and tailor"*) — the department checklist **authoring** editor (the earlier curate-only draft is **superseded**): FieldShore ships a **paraphrase-approved baseline** per checklist; a department **forks and fully authors** it (add/edit/remove/reorder steps + sections + **reset-to-baseline**). **ADR-020 = a Principle-1 SCOPE CLARIFICATION** extending [ADR-002](https://github.com/Vergo402/paratech-struts/blob/v4-redesign/docs/v4-design/11-decisions/ADR-002-principle-1-scope-clarification.md) — *a department authoring its own SOPs is configuration, not the app inventing doctrine; Principle 1 binds FieldShore's baseline, not the dept's tailoring*. Guardrail = **provenance** (a "FieldShore baseline" vs. "Customized by \<dept\>" marker that carries to the attestation screens so a crew knows whose step it is), not restricting authoring. **Admin-only** (no new toggle), **dept owns outright** (no FieldShore review), edits audited (D7.5). **template-vs-instance** load-bearing (Admin authors back-office; fireground roles attest). Lives under Settings → Department policies. Safety engine (load tables/shore types/strut math) is **never** a checklist template (ADR-002/020). Decision locked now; editor UI rides the v4.1 baseline-content timeline (99-OQ #32). OQs: baseline-vs-fork data model, provenance granularity, delegated-author forward hook.
- `09-workflows/30-user-management.md` 🟢 ([#233](https://github.com/Vergo402/paratech-struts/issues/233), Session 5) — the Admin RBAC surface: **Roles ↔ Members** segmented; a role = name + the **~8 back-office permission toggles** (read · run field work · create/end ops · manage inventory · manage roster · manage settings · manage users · export/delete data); **Admin** built-in (all 8, ≥1 always) + editable **Default** + unlimited custom roles; assign/edit-role/promote = reversible sheets, revoke/delete-role = destructive modals (delete reassigns members to Default first); **anti-lockout** (last Admin can't be demoted/revoked — disabled with reason, enforced in data-driven rules); **two orthogonal axes** — back-office role ≠ ICS position, never gates a fireground action; model+rules v4.0, UI flagged (#32). OQs: revoke semantics, permission-key schema, UI ship version, 2FA hook (#33).
- `09-workflows/32-mutual-aid-invite-accept.md` 🟢 ([#235](https://github.com/Vergo402/paratech-struts/issues/235), **v4.0 — [ADR-022](https://github.com/Vergo402/paratech-struts/blob/v4-redesign/docs/v4-design/11-decisions/ADR-022-mutual-aid-v40-qr-guest.md)**; rewritten from the earlier v4.5/shown-disabled draft, #235 reopened) — the verb of [`52-cross-dept-invite.md`](08-information-architecture/52-cross-dept-invite.md): host IC/Admin **generates one incident code = QR + human-readable fallback** (multi-use·revocable·castable to the C-13 board) → arriving officers **scan or type** → land as a **provisioned-dept Member** OR a **walk-up guest** (per-device anon UID, **typed unit tag**, **v3 external-equipment handling — no on-the-fly inventory**, read-only/claimable on close) → host can **revoke** (destructive modal) → **merged multi-agency audit roll-up** in the host's log. **Permission grant, not a message** (no push, Principle 10); **distinct from** the dept-level join (#232). QR display/scan **compose existing primitives** (sheet+image; input camera affordance) — not a 16th primitive; native scanner = 99-OQ #42. **#237 Demo Mode = REJECTED** (dropped at Phase D gate — closed not-planned).
- `09-workflows/31-audit-log-review.md` 🟢 ([#236](https://github.com/Vergo402/paratech-struts/issues/236), Session 5) — **the event log made readable** (ADR-009 — audit log *is* the event log, not a parallel record): scope segmented (All/user/action/time) + filter, newest-first virtualized list (K-15, 1000+ events), per-entry **before→after** inline detail panel (not a sheet); **fully immutable** — no edit/delete, correct state by a new logged action; the **export convergence point** (CSV raw + assembled ICS-201/203/207/208/209 + PAR snapshot + Hazard ICS-208) — the **same packet the #238 after-action auto-email delivers** (ADR-018, on close, records-only); **read+export = IC/Operations only** (#217 — position-gated, visible-but-locked-at-entry, the one hide-not-grey exception); event-log persistence v4.0, review UI flagged (#32). OQs: ICS forms in scope, pagination at scale, immutability enforcement, multi-incident roll-up (v4.5).

### Wireframes (Phase G)

Text based wireframes initially, optionally Figma later.

### Decisions (continuous)

ADRs for every committed choice. Template at `11-decisions/ADR-template.md`.

---

## How to Use This Folder

- **New session opens v4 work** → read `00-INDEX.md` (this file), `01-context.md`, `02-principles.md`, then jump to the phase status row to find current work.
- **Need to make a design decision** → write an ADR using the template. Commit it before implementing.
- **Need to know what's not yet decided** → read `99-open-questions.md`.
- **Want to understand the standard of detail for design work** → read `03-primitives/picker.md` as the worked example.

---

## Strict Rules

1. **Real names are allowed under nominative fair use** (the legal doctrine that lets you name a competitor's product when comparing truthfully)**.** Describe behavior, not brand. Never disparage. Cite truth. No trademarked taglines presented as endorsements. See ADR-001. (Previous rule: codenames only, relaxed 2026-05-21.)
2. **Nothing in here is committed to `main`.** Everything stays on the `v4-redesign` branch until Phase J cutover.
3. **Every committed decision becomes an ADR.** No silent design changes.
4. **The plan file (`v4-master-plan.md`) is the constitution.** This folder is its execution.
