# Design Exploration Queue

Open design ideas from the FieldShore v4 board (epic **#383 — [Design] Exploration
backlog**). This file is a **projection of the board**, regenerated by the
`/ship-design-ideas` skill — do not hand-edit; capture ideas with `/design-idea` and
re-ship.

When this queue has items, build a mockup for each using the real FieldShore
components in this project. Each item links its board issue; when an idea is
resolved its issue is closed and it leaves the queue on the next ship.

---

_No open ideas yet. Captured ideas (`/design-idea "…"`) appear here after the next
`/ship-design-ideas`._
